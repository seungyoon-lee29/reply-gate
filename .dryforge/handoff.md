# 사이클 3 핸드오프 — 검색 품질 (RAG 심화) (governing doc)

## 문서 역할과 충돌 해소

| 문서 | 역할 | 권위 |
|---|---|---|
| `.dryforge/spec.md` | **무엇을** — 행동 계약, ground truth | 충돌 시 항상 이긴다. 오류는 사용자 승인으로만 수정 |
| `.dryforge/plan.md` | **어떻게·순서** — 태스크와 실행 그래프 | 잠정 청사진. 자유롭게 수정 가능 |
| 기존 코드 | HOW 참고 + 현실 대조 | WHAT 의 권위가 아니다 |

프로젝트 컨텍스트는 harness(`CLAUDE.md`/`AGENTS.md` + `docs/`)가 들고 있다 —
이 핸드오프는 이번 사이클 고유의 통제만 담는다.

## 실행 형태

태스크 12개, 파도 11개(그래프 참조). 작업 전 필독은 harness 규칙 그대로:
`docs/standards.md` → `docs/engineering-notes.md` → 손댈 모듈의 `AGENTS.md`,
그리고 작업 종류별 표(`CLAUDE.md`). 이번 사이클은 **벡터 검색·임베딩** 행과
**평가·지표** 행이 둘 다 걸린다.

**T1–T7·T9 는 끝났다. T8 은 해당 없음.** 남은 것은 **T10·T11·T12** 이고, 남은 과금 지점은
**T10(라이브 3회) 하나**다 — T7 은 오프라인이었고, 정책 재색인은 **볼륨을 다시 지우지
않는 한** 다시 돌 일이 없다.

## 이 문서를 유지하는 방식

**어떤 절의 주장이 커밋·문서로 대체되면 그 절을 해시 한 줄로 줄인다.** 사이클 끝을 기다리지
않는다. `.dryforge/` 는 버전관리 대상이므로(하드 게이트 13) 지운 절은
`git log -p -- .dryforge/handoff.md` 에 그대로 남고, 남겨 두면 매 compact 마다 전문이 다시
주입되는 비용만 낸다. 남기는 기준은 하나다 — **다음 사람이 알아야 하는데 코드도 git 도
말해주지 않는 것.** 지우기 전에 갈 곳이 있는지 본다: 트레이드오프 판단 → `docs/tracking/decisions/`,
지금 못 고치는 것 → `findings.md`, 실측·기준선 → `status.md`, 함정·방법론 →
`docs/engineering-notes.md`.

**이 규칙은 인계 게이트 훅이 함께 요구한다** — 갱신 지시가 "덧붙이기가 아니라 대체하기"라고
말하고 현재 크기(줄 수·KB)를 실어 준다. 32KB 를 넘으면 줄이라고 재촉하고, 128KB 를 넘으면
스냅샷을 만들 수 없어 compact 가 막힌다.

## 지나온 것 — 서사는 커밋과 문서에 있다

| 시점 | 무엇 | 커밋 | 지금 그 내용을 들고 있는 곳 |
|---|---|---|---|
| 08-13 전반 | 유료 실행 전 하네스 수정 (블라인드 적대리뷰 4회가 채택 판정 축의 구조 결함을 찾음) | `6fe899c` | [결정 0009](../docs/tracking/decisions/0009-채택-판정을-절대-축으로-통일한다.md) |
| 08-13 후반 | 비교 입력 3종 확정 (정답 관련성 경계 · oracle 정합 · blind 재작성 모델 재생성) | `6fe899c` | [결정 0010](../docs/tracking/decisions/0010-검색-정답의-관련성-경계를-정하고-blind-재작성을-모델이-만든다.md) |
| 08-13 심야 | T6 격자 비교 **실측 완료** (과금선을 넘었다) | `121f284`·`1eacded` | `docs/tracking/status.md` + `reports/retrieval-strategies-live-*` |
| 08-14 새벽 | 사이클 1·2 코어 적대리뷰 + 치명 수정 4건 | `b211f54`·`a2fd32e` | 아래 "고친 것" 표 + `docs/engineering-notes.md` |
| 08-14 | **T7 — 재작성 배선 + 컷 0.50 + 검색 계열 토큰 분리** (+ 근사 인덱스 결함 제거) | `1691d22` | `docs/tracking/status.md` "채택과 제품 배선" — **여기가 현재 상태다** |

**결정 0009 는 읽고 시작하라.** 이 사이클 하네스 계약이 바뀐 지점이고 되돌리면 안 된다:
채택 판정은 전 전략이 코사인 절대 축 하나를 쓰고, 순위 품질(recall@k)은 컷과 분리해 전체
순위로 계산한다. 나머지 계약(`fusion_pool_size` > `rerank_top_n` 강제, 도달 가능성을 검사하는
RRF 컷, 재작성의 3개 다리 적용, `paid_rerank` 와 `live` 의 분리, 픽스처 경로와 결속된 재작성
조건, 실패 귀인 5분류)은 전부 코드와 테스트가 강제한다.

**T8(채택 구성 DB 반영)은 돌지 않는다.** 임베딩 모델 상향을 채택하지 않아 차원이 그대로다.
처리 기록 스키마는 T7 이 넓혔지만 **볼륨을 재생성하지 않았다** — 컬럼 추가는 멱등 ALTER 로
살아 있는 볼륨에 붙였다(사용자 확정). **이 사이클에 실제로 돈 재색인은 T8 이 아니다** —
provenance 컬럼이 스키마에 들어가면서 볼륨을 재생성했고 그 복구로 한 번 돌았다.
T10 은 실행 직전에 한 번 더 확인받는다(하드 게이트 8).

## 진행 상태 — **T7 완료, 라이브 과금 게이트 앞에서 대기 중**

### 다음 세션의 **첫 행동 하나** — T10 승인 받기

**T7 은 끝났다** (`1691d22`). 재작성 배선 + 컷 0.50 + 검색 계열 토큰 분리 + 폴백 기록이
한 커밋으로 나갔고, 채택 근거·바뀐 기본값은 `docs/tracking/status.md` "채택과 제품 배선"
이 들고 있다. 열려 있던 설계 선택(재작성 자리)은 사용자가 **(A) 별도 호출**로 확정했다 —
근거는 그 절과 findings 11 에 적혀 있다.

**남은 것은 T10·T11·T12 이고 전부 (c) 라이브 과금 게이트 뒤다.** T10 은 실행 직전에 한 번
더 확인받는다(하드 게이트 8). 확인 없이 `--live` 를 돌리지 마라.

#### T10 을 돌리기 전에 알아야 하는 것

- **DB 는 `docker compose up -d --wait` 로 올리기만 하면 된다** — 적재는 볼륨에 남아 있고
  검색 계열 컬럼도 멱등 ALTER 로 이미 붙어 있다. **볼륨을 지우면(`down -v`) 정책 재색인이
  다시 과금이다.** 날려야 하는 상황이면 **날리기 전에** 사용자에게 올려라(하드 게이트 8·9).
- **사이클 1·2 라이브 수치와 직접 비교할 수 없는 이유가 셋으로 늘었다**: ① L1 PII 검출률
  정의가 넓어졌고 ② 컷이 0.30 → 0.50 이고 ③ **근사 벡터 인덱스가 검색을 조용히 잘라먹던
  결함이 T7 에서 제거됐다**(아래). ③ 은 재현 조건이 "죽은 튜플 누적"이라 과거 실측이 얼마나
  영향받았는지 산출물로 되짚을 수 없다 — T10 이 새 기준선이다.
- **남은 A4 2건은 그대로다**(`GROUP BY` 별칭이 시스템 컬럼 검사를 가림 · 같은 `WITH` 안
  중복 CTE 이름). 사용자가 등급 동결을 확정했다.

#### T7 이 함께 고친 것 — **다음 사람이 놀랄 자리**

`policy_chunks` 의 HNSW 인덱스를 **제거**했다. 근사 인덱스라 `top_k=5` 를 요청해도
**1~2행만, 그것도 1위가 빠진 채** 돌려줬다(26행 테이블·임계값 0.0 에서 실측). 조항 26개에서
정확 스캔은 0.1 ms 미만이라 인덱스가 사는 이유가 없다. 진단 과정과 두 번의 헛짚음은
`docs/engineering-notes.md` "근사 인덱스가 검색을 조용히 잘라먹었다" 에 있다.

**이 결함이 남긴 규칙 하나**: 이 계열은 오류가 아니라 **업무 판정으로 위장한다**
(검색이 짧아지면 `no_evidence` 가 늘고 그 수가 "검색 품질"로 리포트에 실린다). 낡은 인덱스
건(findings 16)과 같은 모양이고, **지표가 유일한 주장인 제품에서 가장 나쁜 실패 모양**이다.
검색 층에 무언가를 더할 때는 "이것이 조용히 적게 돌려줄 수 있는가"를 먼저 물어라.

### 고친 것 — 서사는 커밋과 `docs/engineering-notes.md` 에 있다

| 무엇 | 커밋 |
|---|---|
| 치명 4건 — SQL 원문 세탁 · 출력 별칭이 연 두 번째 세탁 · PII 표기 변형 6종 · 빈 `claim.text` | `b211f54` |
| 오탐 — 답변에 나가지 않는 키의 PII 로 정상 초안 기각 | `e1bce47` |
| L2 미판정이 "게이트가 놓쳤다"와 같은 값으로 세어짐 | `53f11f2` |
| 동일 텍스트 claim 이 판정 1건으로 접힘 (+ 옳은 산출이 "중복"으로 거부됨) | `8c0f30f` |
| 주입 SDK 재시도 곱셈 + `attempts` 실측화 (SQL 경로 포함) | `cddbd87`·`7bb05e9` |
| 정책 벡터의 임베딩 출처 부재 → provenance 기록 + 다른 공간 질의 거부(503) | `42a50c8` |
| 의존성 해석이 커넥션을 먼저 열어 422·503 이 500 에 가림 | `7332248` |

**⚠ L1 PII 검출률의 정의가 넓어졌다.** 사이클 1·2 라이브 수치와 직접 비교할 수 없다.

### 이 건들이 남긴 방법론 — **`docs/engineering-notes.md` 로 옮겼다**

"결함 보고서를 그대로 티켓으로 옮기지 마라 — 먼저 재현하라" 절을 보라. T7 의 검색 결함
진단에서 **내 가설이 두 번 틀린 사례**가 여덟 번째 항목으로 들어갔다. 남은 A4 2건과
T10 이후 작업에도 그대로 적용된다.

### 아직 안 고친 것 — **둘 다 A4, 사용자가 등급 동결을 확정했다**

- **`GROUP BY` 별칭이 시스템 컬럼 검사를 가린다.** `SELECT ctid` 는 `unknown_column` 으로
  거부되는데 `SELECT count(*) AS ctid ... GROUP BY ctid` 는 통과한다. engineering-notes 의
  "별칭이 컬럼 화이트리스트를 무력화했다"가 **`HAVING` 은 제외한다**고 명시하며 고친 계열인데
  `GROUP BY` 가 남았다 — Postgres 는 둘 다 맨 이름을 입력 컬럼으로 먼저 해석한다.
  **다만 이 경로로 시스템 컬럼 값이 출력되는 쿼리는 만들지 못했다** — 심층 방어 침식이지
  활성 유출이 아니다. 등급을 올리지 마라.
- **같은 `WITH` 안 중복 CTE 이름이 가드를 통과한다.** 다만 PostgreSQL 이 그 쿼리 자체를
  거부하므로 데이터는 새지 않는다. fail-closed 원칙 위반이지 유출 경로가 아니다.

### 사용자 판단 사안 (코드 수정이 아니다)

- **목표 75% 가 새 라벨에선 L2 없이도 달성된다**(L2-off 기준선 86.7%). D 슬라이스 3명 전원.
  `evaluation.TARGETS` 주석에 폐기된 전제가 그대로 남아 있다. 결정 0006 갱신은 T11 이다.
- **결정 0007 의 "배치만이 모순 감지를 보존한다"가 false dichotomy** — claim별 support 호출
  + 전체 근거 contradiction 호출로 나누는 hybrid 를 검토하지 않았다. J11 이 그 비용이다.
- **J11 라벨 자체가 틀렸을 수 있다** — C 슬라이스 3명이 독립적으로 "환불 7일 vs 반품 14일이
  같은 사안인지 입력에 명시되지 않는다"고 지적했다. 판정기가 옳고 픽스처가 틀렸을 가능성.
- **`bait_reject_recall` 이름이 과장이다** — 분자가 `rejected_at_least_once`(층·사유 무관)라
  26.7% 는 미끼 재현율이 아니라 "reject_bait 범주의 any-layer 기각률"이다.
  findings 1번이 "`pii_detected` 는 0/15"라고 이미 정직하게 적어 두긴 했다.
- **범위 결정 2건이 findings 에서 대기 중이다** — [16](../docs/tracking/findings.md)
  (**빈** 정책 인덱스는 아직 `no_evidence` 로 위장한다. 낡은 인덱스만 503 이 됐다)과
  17(조회 라우트가 쓰지도 않는 RO 계정에 묶여 있다). 둘 다 "안 고치는 이유"까지 적혀 있고
  고르는 것이 사용자 몫이다. **T7 에 끌어들이지 마라** — 검색 배선과 무관하다.

### T7 채택 범위 — **배선 완료 (`1691d22`)**

채택 근거·미채택 사유·바뀐 기본값 표는 `docs/tracking/status.md` "채택과 제품 배선"이
들고 있다. 여기 남기는 것은 **되돌리면 안 되는 결속** 하나뿐이다:

**⚠ 컷과 재작성은 한 짝이다.** `vector_similarity_threshold` 0.50 은 재작성이 유사도를
밀어 올린 뒤의 값이다. **재작성을 끄면서 컷을 유지하면 recall 이 0.880 → 0.640 으로
무너진다.** `QUERY_REWRITE_ENABLED=false` 로 돌릴 일이 생기면 컷도 함께 되돌려라.

### 사이클 1·2 라이브 수치 — **구 설정 기준선으로 강등** (사용자 확정)

산출물은 사후 편집하지 않는다(findings 9 원칙). `status.md` 에 **"구 L1 정의 · 컷 0.30 기준"**
이라고 명시하고, L2 기여도 분해는 새 계열에서 다시 쌓는다. 치명 수정이 L1 의 PII 탐지 정의를
넓히므로 **컷과 무관하게 이미 재측정 대상**이다 — "비교 보존"은 더 이상 컷 유지의 논거가 아니다.

### 이 리뷰가 남긴 방법론 (사이클 4 로 가져갈 것)

- **적대리뷰를 상설 게이트로 올려라.** 사이클 3 의 완성도 차이는 설계가 아니라 리뷰에서 왔다.
  안 돌리면 사이클 4 는 같은 자리로 돌아간다.
- **리뷰의 수확은 버그가 아니라 "숫자가 라벨이 말하는 것을 뜻하지 않는다"였다.** 이 계열은
  테스트로도 라이브로도 안 잡힌다 — 전부 녹색이고 전부 숫자를 낸다.
- **이미 관대하게 설명해 둔 이상 신호를 공격 대상으로 지목하라.** `pii 0/15` 와 `J11` 을
  루브릭에 박았더니 리뷰어 7명이 독립적으로 그 설명을 깼다.
- **보안 슬라이스는 Codex 제공자 필터에 걸릴 수 있다.** 12회 중 2회가 그렇게 죽었다.
  재시도하거나 Claude 서브에이전트로 그 슬라이스만 돌리는 우회가 필요하다.

## 하드 게이트 (실행 에이전트가 코드에서 유도할 수 없는 불가침)

1. **게이트 두 층은 무변경이다.** `gate.py`·`judge.py` 의 동작, 사유 4종·2종, L1 의
   LLM·네트워크 import 금지, L2 의 배치·fail-closed. 이번 사이클은 **게이트 앞**만
   손댄다. 검색이 좋아졌다고 판정을 느슨하게 하지 마라.

2. **재작성 질의 픽스처는 런타임으로 새면 안 된다.** `data/rewritten_queries.jsonl` 은
   오프라인 비교 전용이다. 실행 경로가 읽으면 골든셋 30건 전용 치트가 되고 측정 2 의
   수치가 통째로 무의미해진다. **구조 테스트로 못박고, 그 테스트가 실제로 실패하는지
   확인하라**(참조를 일부러 넣어 빨개지는 것을 본 뒤 되돌린다).
   **T7 에서 확인했다** — 새 런타임 모듈 `query_rewrite.py` 는 검사 대상에 자동으로 들어왔고,
   픽스처 경로 상수를 넣으니 빨개졌다. 다만 **그 검사는 문자열 상수까지 본다** — 런타임
   모듈의 주석·docstring 에 픽스처 파일명을 적어도 걸린다. 가드를 느슨하게 하지 말고 문장을
   바꿔라.

3. **검색 단계 실패는 폴백이고, 배선 누락은 조립 오류다.** 둘을 섞지 마라.
   런타임 호출 실패(재작성·리랭크) → 폴백해서 계속. 전략 켜짐 + 클라이언트 미배선 →
   조립 시점에 명시적으로 죽는다. **L2 호출 실패가 인계인 것은 변함없다.**

4. **조용한 폴백 금지 · 토큰 은폐 금지.** 폴백은 처리 기록과 리포트에 남는다. 실패한
   호출의 토큰도 실비용이므로 센다. 검색 단계 생성 토큰은 생성·임베딩·판정 어디에도
   합산하지 않는다.

5. **평가 장치 제거 금지 — 목록이 둘 늘어난다.** 기존(정책 문서의 미끼·모호·상충 조항,
   골든셋, `l1_fixtures.jsonl`, `judge_fixtures.jsonl`)에 더해
   **`data/retrieval_labels.jsonl`** 과 **`data/rewritten_queries.jsonl`** 이 같은
   자격으로 들어온다. 라벨이 사라지면 이번 사이클 모든 수치의 분모가 사라진다.

6. **라벨은 채점자만 본다.** 어떤 검색 전략도 정답 라벨을 입력으로 받지 않는다.
   비교 하네스 안에서 둘이 가까이 있어 실수로 새기 쉽다 — 테스트로 지켜라.

7. **산출물 보존.** 기존 라이브 실측 6회분 **12개 파일**(`reports/evaluation-live-{1,2,3}`
   · `evaluation-live-l2-{1,2,3}` × `.md`·`.json`)은 덮어쓰지도 지우지도 않는다.
   새 검색 비교 리포트(`reports/retrieval-*`)는 git 추적 대상이고, 추적되는 순간
   저장소의 PreToolUse 훅이 Edit/Write 경로의 사후 편집을 막는다. **훅이 못 잡는 경로가
   하나 있다** — 비교 스크립트 재실행이다. 그래서 하네스가 기존 산출물에 쓰기를 스스로
   거부해야 한다(spec §14).

8. **과금은 명시적으로만.** 상한 $10 안에서 오프라인 비교·재색인은 승인 없이 돈다.
   **라이브 재실측(T10)은 실행 직전에 한 번 더 확인받는다.** 상한을 넘길 상황이면
   실행 전에 멈추고 보고한다. 비밀은 `.env` 에만 — 로그·리포트·오류 메시지에 값 금지.

9. **볼륨 재생성 전 사용자 확인 + 처리 기록 백업·복원.** 차원 변경이나 스키마 확장이
   채택에 포함되면 T8 의 절차를 그대로 탄다. 복원은 **API 로 실제 열어** 확인한다 —
   건수 대조만으로는 부족하다.

10. **미달이면 원복이 기본이다.** 라이브가 오프라인 채택을 반증하면 기존 기본값으로
    되돌리고 실측·원인을 남긴다. **실측이 정당화하지 않는 기본값을 저장소에 남기지
    않는다** — 남기면 "측정해보니 충분해서"라는 채택 근거 전체가 무너진다.

11. **미실행·미측정을 0 으로 채우지 않는다.** BGE-M3 미설치, 라벨 부재, 중단 — 전부
    "미실행 + 사유"다. 기존 리포트 불변식 그대로다.

12. **검증 게이트**(병합 전): `ruff check` · `ruff format --check` · `mypy`(strict) ·
    `pytest` 전체 녹색 + `pytest -m db` **skip 0**. DB 를 재생성했다면 재생성 상태에서
    다시 확인한다.

13. **`.dryforge/` 는 이 저장소에서 버전관리 대상이다 — gitignore 하지 마라.**
    **이 3-doc 은 이미 커밋돼 있다(`da4c6a1`).** 프로듀서가 untracked 로 넘기는 것이
    일반적인 인계 상태지만 여기서는 아니다 — 추적 상태가 정상이고, 더러운 작업 트리도
    이상 징후도 아니다. 이 사실 때문에 멈추지 마라.
    `.gitignore` 는 `.dryforge/worktrees/` 와 `.dryforge/backup/` 만 무시하고, 그 자리에
    "3-doc 본문은 버전관리 대상"이라고 적혀 있다. 사이클 1·2 의 3-doc 이 실제로
    `.dryforge/001/`·`002/` 로 커밋돼 있다.
    - 이번 3-doc 은 사이클 끝에 **`.dryforge/003/` 로 옮겨 커밋**한다(앞 두 사이클과 같은
      방식). 작업 중에는 루트의 `spec.md`·`plan.md`·`handoff.md` 그대로 둔다.
    - **`git rm -r --cached .dryforge/` 를 실행하지 마라.** 이전 사이클이 남긴 추적 상태를
      정리하는 기본 절차지만, 여기서는 **보존 대상인 001·002 를 통째로 추적 해제한다.**
    - 이 항목은 실행 하네스와 무관하다(Claude Code · Codex 어느 쪽이든 같다).

14. **이 사이클은 설계와 실행이 다른 세션·다른 하네스에서 돈다.** 설계 대화는 넘어오지
    않는다 — **이 3-doc 이 전부다.** 그래서 판단이 갈리는 지점을 이 문서에 박아 뒀다.
    문서로 풀리지 않는 것이 나오면 **추측하지 말고 사용자에게 올려라.** 설계 세션에
    물어볼 수 없다는 사실이 escalate-don't-guess 를 더 엄격하게 만든다, 느슨하게가 아니라.
    - Codex 세션에서도 `reports/` 아래 커밋된 산출물은 훅이 막는다
      (`.codex/config.toml` → `.claude/hooks/protect-live-reports.sh codex`).
      막히면 우회하지 말고 그 사실을 보고하라 — 그게 훅의 목적이다.

## 작성 중 확정됐지만 spec/plan 에 자리가 없는 의도

**T7 이 소진한 항목은 지웠다** (재작성 자리·프롬프트 단일 소유·합집합·폴백 규칙 → 코드와
`docs/`; 리랭커 계열·BM25 토큰화·인메모리 비교 → 오프라인 하네스에만 남아 더 이상 판단
지점이 아니다; 외부 SDK 규약 확인 → T6·T7 에서 끝났다). 남은 것은 **T10·T11·T12 가 실제로
부딪힐 것들**이다.

- **이 사이클의 판정 기준은 "측정해서 보여주기"가 아니라 "실제로 좋아지기까지"다.**
  사용자가 그렇게 골랐다. 다만 **어떤 단도 표적 결함을 개선하지 못하면 기본값을 바꾸지
  않는 것**이 올바른 결말이고, 그때는 그 사실이 이 사이클의 산출이다 — 억지로 채택하지
  마라. **T10 이 오프라인 채택을 반증하면 원복이 정상 결말이다**(하드 게이트 10).

- **성공 판정 ②를 "생성 토큰 0"으로 재지 마라.** 의도 해석 호출이 검색보다 먼저 무조건
  돌고 그 토큰이 `input_tokens`/`output_tokens` 에 들어간다 — 커밋된
  `reports/evaluation-live-l2-1.json` 의 G17 이 이미 `no_evidence` 인계인데
  `input_tokens 216` 이다. 판정 기준은 **시도(초안) 0건 + 판정 토큰 0** 이다(spec §10-2).
  **T7 이 검색 계열을 분리했지만 그것도 이 정의를 바꾸지 않는다** — 재작성 토큰은
  `retrieval_input`/`retrieval_output` 이라는 별도 키에 있고 생성 합산에는 없다.

- **G17 은 "검색이 성공하면 답변으로 끝나도 정답"이다.** 골든셋 라벨이 `answered` 와
  `no_evidence` 인계를 둘 다 허용한다(결정 0008). 표적 관측 ①은 **일치율이 아니라 채택
  근거에 `policy:support:4-1` 이 있는가**로 판정한다 — T9 가 넣은 `adopted_evidence_ids`
  필드가 그 자리다.

- **검색 개선이 성공하면 지금 가장 안정적인 L2 라이브 기각 장면이 사라진다.** 무근거 4건
  (G21–G24)이 검색 단계에서 걸러지면 그 12/12 인계는 더 이상 L2 장면이 아니다. 사용자는
  **새 서사로 재구성**을 골랐다 — "검색이 먼저 거르고, 남은 의미 결함은 L2 가 잡는다".
  L2 라이브 재현은 **G18(모순 기각, 사이클 2 에서 3/3)** 이 맡는다. 이 장면을 살리려고
  검색을 일부러 느슨하게 두지 마라.

- **목표치 재확정은 이번 사이클 실측 뒤로 미뤄 둔 상태다.** 새 라벨 기준으로는 L2 꺼짐
  기준선(86.7%)도 목표 75% 를 넘어 목표가 이미 현황 추인이 됐다(결정 0008 위험 절).
  T11 에서 결정 0006 갱신으로 닫는다. **그 전에 임의로 숫자를 올리지 마라.**

- **리포트 이름 계열을 늘리지 않는다.** 검색 구성은 같은 지표를 다른 설정으로 잰 것이라
  **실행 조건 필드**가 맞는 자리다(T7 이 `retrieval_strategy`·`embedding_dimensions` 를
  넣었다). L2 켜짐 실측의 자동 넘버링(`evaluation-live-l2-<n>`)을 그대로 쓴다. **이 판단이
  틀렸다고 보이면 임의로 바꾸지 말고 사용자에게 올려라** — 산출물 이름 규칙은 이 저장소가
  한 번 사고를 낸 자리다(`docs/engineering-notes.md`).

- **`build_pipeline` 의 판정자 주입 구멍(findings 10)을 고치지 마라.** 공개 표면 변경이고
  별도 설계가 필요하다고 이미 판단해 둔 항목이다. T7 이 `EvidenceCollector` 에 재작성
  클라이언트를 주입하면서 **대역 조립기와의 드리프트 면적이 한 칸 넓어졌다** —
  `tests/test_testing_doubles.py` 가 두 조립의 재작성 배선까지 대조해 그 칸을 덮는다.

## 완료의 정의

**게이트가 셋이다**(spec §15). **(a) 비교 준비**(T1–T5·T9)와 **(b) 채택과 제품 배선**
(T6·T7, T8 은 해당 없음)은 **끝났다** — (b) 의 조건부 항목("폴백·미배선 테스트는 채택
구성이 검색 단계에서 생성 호출을 쓸 때만")은 재작성이 생성 호출을 쓰므로 **해당됐고**
`tests/test_query_rewrite.py` + `tests/test_evidence.py` 가 덮는다.

**남은 것은 (c) 실측과 판정 — 라이브 과금(별도 승인).** T10·T11·T12.

표적 관측이 재현되지 않으면 무한정 붙잡지 말고 시도 내역과 수치를 그대로 보고해
사용자 판단을 받는다. **원복도 정상 결말이다.**
