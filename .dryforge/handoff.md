# 사이클 3 핸드오프 — 검색 품질 (RAG 심화) (governing doc)

## 문서 역할과 충돌 해소

| 문서 | 역할 | 권위 |
|---|---|---|
| `.dryforge/spec.md` | **무엇을** — 행동 계약, ground truth | 충돌 시 항상 이긴다. 오류는 사용자 승인으로만 수정 |
| `.dryforge/plan.md` | **어떻게·순서** — 태스크와 실행 그래프 | 잠정 청사진. 자유롭게 수정 가능 |
| 기존 코드 | HOW 참고 + 현실 대조 | WHAT 의 권위가 아니다 |

프로젝트 컨텍스트는 harness(`CLAUDE.md`/`AGENTS.md` + `docs/`)가 들고 있다.
유지 규칙: **대체된 절은 즉시 해시 한 줄로 줄인다.** 남기는 기준은 "다음 사람이 알아야
하는데 코드도 git 도 말해주지 않는 것" 하나다. 32KB 초과 시 축소, 128KB 초과 시 compact 불가.

## 지나온 것 — 서사는 커밋과 문서에 있다

| 시점 | 무엇 | 커밋 | 내용이 있는 곳 |
|---|---|---|---|
| 08-13 | 하네스 수정 + 비교 입력 3종 확정 | `6fe899c` | 결정 0009·0010 |
| 08-13 심야 | T6 격자 비교 실측 | `121f284`·`1eacded` | `status.md` "검색 전략 비교" |
| 08-14 새벽 | 사이클 1·2 코어 적대리뷰 → 치명 수정 4건(`b211f54`·`e1bce47`·`8c0f30f`·`7bb05e9` 등) + 훅 확장(`a2fd32e`) | 좌기 | `docs/engineering-notes.md` |
| 08-14 | T7 재작성 배선 + 컷 0.50 (+HNSW 결함 제거) | `1691d22` | `status.md` "채택과 제품 배선" |
| 08-14 | **T10 라이브 3회** → 표적 ①② 충족·가드 미달(G04) → **컷만 0.30 원복(재작성 유지)** | `12539bd`·`72ff2d4` | `status.md` "T10 라이브 재실측" + findings 18·19 + 결정 0011 |
| 08-14 | **T11 문서 현행화** (목표치 재확정 = 결정 0006 갱신) | `4dd7455` | 각 문서 |
| 08-14 | **T10·T11 적대리뷰** (사용자 지시) — 공격 6슬라이스 완료, finding 35건 | (미커밋) | 아래 절 — **여기가 현재 상태다** |

## 진행 상태 — **적대리뷰 검증 단계 실행 중**

### 지금 하던 것

T10·T11 산출물 적대리뷰. 새 컨텍스트 서브에이전트 6개(페르소나 분할 없음 — 사용자 저장
방식)가 공격을 마쳤고 finding 35건이 나왔다. 그중 **7건은 워크플로 자체 검증이 CONFIRMED**,
**major 전부는 메인 세션이 기계 재현으로 확인**, 남은 **19건의 독립 반박 검증 워크플로가
지금 돌고 있다**: run `wf_ef00e18f-abb`, task `w3hod8qdd`. 검증자는 무과금(로컬 명령만)이다.

**리뷰 결과는 아직 저장소 어디에도 없다** — 유일한 사본이 세션 태스크 출력이다:
- 공격 전문(6슬라이스·35건+clean_checks): `/private/tmp/claude-501/-Users-ian-workspace-reply-gate/d0ec48b2-5346-49f6-99ff-4729dafec942/tasks/wylhmqjpx.output`
- 1차 실행분(2슬라이스): 같은 경로 `tasks/wnez8djgd.output`
- 미검증 19건 추출본: 같은 경로 `scratchpad/unverified_findings.json`
- 검증 결과(완료 시): 같은 경로 `tasks/w3hod8qdd.output`

### 다음 첫 행동 하나

`tasks/w3hod8qdd.output` 을 읽어 19건 판정(CONFIRMED/REFUTED/PARTIAL)을 기확인 7건과 합쳐
**26건 최종 판정표를 사용자에게 보고**하고, 아래 처분 묶음의 선택을 받는다. 완료 통지가
안 왔으면 그 태스크가 아직 도는 것이다 — **공격 슬라이스를 재실행하지 마라**(아래 함정).

### 적대리뷰가 확인한 것 — 처분 대기 (사용자 선택 전, 아무것도 반영하지 않았다)

판정 자체는 전부 버텼다(6개 슬라이스가 T10 수치·가드 기준선·토큰 산식을 독립 재현).
무너진 곳은 서사의 집계와 T11 회계다:

| 묶음 | 내용 | 승인 필요 |
|---|---|---|
| A. spec 2곳 개정 | §3-1 이 라벨(G16·G17=4-1만, 결정 0010)과 모순 / §10-3 원복 정의가 실제(재작성 유지, 결정 0011)와 모순 — 냉시작이 spec 을 따르면 사용자 결정이 뒤집힌다 | **예 (spec 수정)** |
| B. 서사 정정 | ① **잃은 케이스는 둘이 아니라 셋** — G01 도 상충 조항 2-2(0.4693)를 잃어 사이클 2 의 2/3 회 모순 기각이 소멸(3/3 귀인 절 기록) ② **run6 G21 탈출 기전 미서술** — 라이브 재작성 변동이 인접 조항 5개를 컷 위로 올림 → L2 기각 → 재생성 구조 위반 → L1 기각(11/12 의 유일 실패) ③ **"L2 기각 장면 소멸"은 과장** — G30 이 3/3 회 `unsupported_claim` 기각→재생성→통과, G02 run6 은 `contradictory_evidence`(상충 장치 작동) ④ 원복 조합의 오프라인 수치(0.30 에서 rewrite R=1.000/P=0.230, vector R=0.880/P=0.238) 미인용 ⑤ p50 "12.4초→"는 최대 단일값(11.1–12.4 범위가 맞다) | 아니오 |
| C. T11 잔여 | `docs/standards.md` 하드 게이트 3 이 아직 "LLM 산출물 넷"(CLAUDE.md 는 다섯 — 모순) / 결정 0009 미확정·낡은 절(0010 이 닫은 것을 미결로 가리킴) | 0009 에 케이스 하한 명시 여부만 판단 |
| D. handoff 정정 | 이 갱신에서 반영했다: T11 완료 상태·a2fd32e 행 배치·하드 게이트 1·2 문구 | 반영됨 |
| E. 잔손질 | findings 1번 절의 11번 앵커 깨짐(제목 변경 탓) / problem.md:204 "§1 의" 오인용(spec §1 의 문장) / test_query_rewrite.py:247 "컷 0.50" docstring | 아니오 |
| F. 하네스 개선 기록 | ① awre 7~8건의 몸통은 컷이 아니라 **의도 라우팅**(채택근거 sql: 뿐·검색 토큰 0 — 정책 검색이 안 돌았다) — 귀인 절이 "걸렀다/안 돌았다"를 구분 못 함 ② G28 이 3회 전부 anomaly(계약상 정상인 사전 인계를 분류기가 모름) ③ "generation_issue" 3건 전부 최종 answered(분류명 오도) — findings 신설로 기록, 수정은 다음 사이클 | 아니오 |

Note 급(README RAG 행 낡음→T12 소유 · "생성 토큰 208~213"은 input 만 · 토큰 절감 기준선의
HNSW 불확실성 병기 · 폴백 0건의 분모는 90 아닌 ~60 · plan T6 보류 서술 낡음)은 전문 파일에 있다.

### 미결 결정 (전부 사용자 몫)

- **처분 묶음 A–F 중 무엇을 반영할지** — 위 표. A 는 spec 수정이라 별도 승인.
- **결정 0009 에 "라벨 있는 케이스가 채택 근거 0건 금지" 하한을 명시할지.**
- **원복 구성(재작성+0.30)의 라이브 재실측 여부** — 3회 ≈ 46만 토큰. 안 사면 문서는 "각 축은
  측정됐고 조합은 미측정"(오프라인 수치는 위 B-④ 로 인용 가능).
- **T12 데모 장면** — 리뷰가 판을 바꿨다: **G30 이 3/3 재현되는 L2 기각→재생성→통과 장면**이다
  (sql 근거만으로 뒷받침 안 되는 claim 기각). 다만 평가 하네스는 기록을 안 남기므로 DB 에는
  없다(findings 19 의 유실과 별개) — 데모용 기록을 만들려면 웹 폼 재현(과금·비결정론)이 필요하다.

### 함정

- **공격 슬라이스를 resume 으로 재실행하지 마라.** pipeline 인터리빙 때문에 캐시 프리픽스가
  어긋나 공격이 재롤된다(실제로 1회 발생 — spec-consistency 가 다른 finding 세트를 냈다).
  검증은 전용 워크플로(`wf_ef00e18f-abb`, 19건 args 고정)로 분리해 뒀다.
- **세션 한도에 두 번 걸렸다**(리셋 4:10·9:10). 서브에이전트 대량 병렬은 ~1M 토큰에서 끊길
  수 있다 — 검증류는 소규모 분할이 안전하다.
- 리뷰 대상 4커밋: `12539bd`(T10 리포트 6개) · `72ff2d4`(원복) · `3e6562b`(handoff) ·
  `4dd7455`(T11). 커밋 메시지의 주장도 리뷰 대상이었고 일부(링크 0건 깨짐 등)는 반증됐다 —
  커밋 메시지는 고칠 수 없으므로 정정은 문서 쪽에 남긴다.

## 하드 게이트 (코드에서 유도할 수 없는 불가침)

1. **게이트 두 층의 판정을 느슨하게 하지 않는다.** 사유 4종·2종, L1 의 LLM·네트워크 import
   금지, L2 의 배치·fail-closed. 사이클 3 중 `gate.py`·`judge.py` 수정 4건은 적대리뷰가 찾은
   결함 수정(정의 확장·오탐 제거·대조 강화)이지 완화가 아니다 — 그래서 L1 PII 정의가 넓어져
   사이클 1·2 수치와 직접 비교할 수 없다(`status.md`). 검색이 좋아졌다고 판정을 건드리지 마라.

2. **재작성 질의 픽스처는 런타임으로 새면 안 된다.** `data/rewritten_queries.jsonl` 은
   오프라인 비교 전용. 구조 테스트가 AST 로 못박고 있고 **문자열 상수·docstring 까지 본다 —
   `#` 주석은 못 본다**(AST 밖이다). 가드를 느슨하게 하지 말고 문장을 바꿔라.

3. **검색 단계 실패는 폴백, 배선 누락은 조립 오류.** L2 호출 실패가 인계인 것은 변함없다.
   (`docs/business-rules.md` "검색 단계 실패" 절이 T11 에서 명문화했다.)

4. **조용한 폴백 금지 · 토큰 은폐 금지.** 검색 계열 토큰은 생성·임베딩·판정 어디에도 합산 금지.

5. **평가 장치 제거 금지.** 미끼·모호·상충 조항, 골든셋, `l1_fixtures`·`judge_fixtures`·
   `retrieval_labels`·`rewritten_queries`·`rewritten_queries_oracle`.

6. **라벨은 채점자만 본다.** 어떤 검색 전략도 정답 라벨을 입력으로 받지 않는다.

7. **산출물 보존.** 라이브 실측 9회분 18개 파일 + `retrieval-*` 리포트는 덮어쓰기·삭제 금지.
   훅이 못 잡는 경로(스크립트 재실행)는 하네스가 스스로 거부한다(`resolve_report_stem`).

8. **과금은 명시적으로만.** 라이브 실측은 실행 직전 확인. 상한 $10. 비밀은 `.env` 에만.

9. **볼륨 재생성 전 사용자 확인 + 처리 기록 백업·복원, 복원은 API 로 실제 열어 확인.**
   findings 19 가 이 조항 위반의 실측 사례다(08-13 재생성에서 보존 대상 2건 유실, 미복구).

10. **미달이면 원복이 기본 · 실측이 정당화하지 않는 기본값을 남기지 않는다.** T10 이 실제로
    탔다(`72ff2d4`) — 축별 원복은 사용자 결정이었고 결정 0011 이 들고 있다. **spec §10-3
    문자와의 모순은 미해소다**(처분 묶음 A).

11. **미실행·미측정을 0 으로 채우지 않는다.**

12. **검증 게이트**: `ruff check`·`ruff format --check`·`mypy`·`pytest` 녹색 + `pytest -m db`
    skip 0. 현재 HEAD 기준 809/162 녹색이다.

13. **`.dryforge/` 는 버전관리 대상 — gitignore 금지, `git rm -r --cached` 금지**(001·002 가
    추적 해제된다). 사이클 끝에 3-doc 을 `.dryforge/003/` 으로 옮겨 커밋한다(전례 동일).

14. **설계 세션은 없다 — 이 3-doc 이 전부다. 문서로 안 풀리면 추측 말고 사용자에게.**
    Codex 세션도 `reports/` 훅이 막는다. 막히면 우회하지 말고 보고하라.

## T12 에 남은 계약

- **성공 판정 ②를 "생성 토큰 0"으로 재지 마라** — 의도 해석이 먼저 돈다(T10 실측: 기권
  케이스도 input 208~213 + output 12). 기준은 시도 0건 + 판정 토큰 0.
- **컷 0.50 조건의 수치(판정 -22.9% 등)를 현재 기본값의 수치처럼 싣지 마라.** 원복했다.
- **"G18 이 기각한다"고 재실측 없이 쓰지 마라.** 컷 원복으로 기전상 복구지만 그 조건의 라이브
  실측이 없다. **대신 G30 장면(3/3)이 존재한다** — 위 미결 결정 참조.
- **리포트 이름 계열을 늘리지 않는다.** 실행 조건 필드가 맞는 자리다.
- **`build_pipeline` 판정자 주입 구멍(findings 10)은 고치지 마라** — 별도 설계 사안.
- README 의 RAG 심화 행(820 부근)이 사이클 3 이후 거짓 — T12 가 서사 재구성에서 함께 고친다.

## 완료의 정의

(a)(b) 완료, (c) 중 11번(T10+원복) 완료. **남은 것: (c)12 = T11(완료, `4dd7455`) 뒤의
T12(데모 서사) + 적대리뷰 처분 반영.** 표적 관측이 재현되지 않으면 붙잡지 말고 보고한다 —
원복도 정상 결말이고, 실제로 그렇게 끝났다.
