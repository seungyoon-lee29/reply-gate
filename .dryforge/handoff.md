# 사이클 3 핸드오프 — 검색 품질 (RAG 심화) (governing doc)

## 문서 역할과 충돌 해소

| 문서 | 역할 | 권위 |
|---|---|---|
| `.dryforge/spec.md` | **무엇을** — 행동 계약, ground truth | 충돌 시 항상 이긴다. 오류는 사용자 승인으로만 수정 |
| `.dryforge/plan.md` | **어떻게·순서** — 태스크와 실행 그래프 | 잠정 청사진. 자유롭게 수정 가능 |
| 기존 코드 | HOW 참고 + 현실 대조 | WHAT 의 권위가 아니다 |

프로젝트 컨텍스트는 harness(`CLAUDE.md`/`AGENTS.md` + `docs/`)가 들고 있다 —
이 핸드오프는 이번 사이클 고유의 통제만 담는다.

## 실행 형태

태스크 12개, 파도 11개(그래프 참조). 작업 전 필독은 harness 규칙 그대로:
`docs/standards.md` → `docs/engineering-notes.md` → 손댈 모듈의 `AGENTS.md`,
그리고 작업 종류별 표(`CLAUDE.md`). 이번 사이클은 **벡터 검색·임베딩** 행과
**평가·지표** 행이 둘 다 걸린다.

**과금선은 T6 앞에 있다.** T1–T5 와 T9 는 전부 무과금으로 완주할 수 있어야 한다.
T6(격자 비교)·T8(재색인)·T10(라이브 3회)이 돈이 나가는 지점이다.

**2026-08-12 사용자 확정 상태:** 수정된 검색 정답 라벨 30건과 blind 재작성 30건은
확정됐다. 사용자가 전략 비교는 기다리라고 명시했으므로 T6 는 새 실행 지시가 있을 때까지
보류한다. 이 확정은 비교 입력만 승인하며, 유료 호출 실행 승인이 아니다.

**2026-08-13 갱신:** T6 는 실행 지시를 받아 **완료**됐다(`121f284`). 남은 과금 지점은
T8(재색인)·T10(라이브 3회)뿐이고, T10 은 실행 직전에 한 번 더 확인받는다(하드 게이트 8).

## 진행 상태 (2026-08-13 전반) — 유료 실행 전 하네스 수정 (커밋 `6fe899c`)

**이 절은 기록이다.** 이 시점에서 T6 는 보류였고 유료 호출 0건이었다 — 지금 상태는
아래 "T6 실측 완료" 절이 들고 있다.

### 무슨 일이 있었나

유료 비교를 돌리기 전에 **블라인드 적대적 리뷰 4회**(동일 루브릭 독립 실행, 페르소나 분할
없음)를 `main...HEAD` 전체에 돌렸다. 리뷰어들은 저장소의 "승인됨·검증 완료" 진술과 테스트
통과 사실을 근거로 쓰지 못하는 조건이었다. 4명이 독립적으로 같은 구조 결함에 수렴했고,
그 결함들은 **전부 녹색 상태에서 성립**했다.

핵심은 하나였다: **순위 융합 점수에는 "관련 없음"이 없는데 채택 판정을 그 축으로 옮겨놨던
것.** 그래서 (a) 하이브리드 이후 단이 기권 불가 → 빈 정답 케이스 precision 0 고정,
(b) 리랭크 채택 집합이 하이브리드와 항등 → 지표 5개 중 4개가 상수, (c) precision 분모가
전략마다 달랐다(대역 13 vs 30).

수정 근거와 기각한 대안은 [결정 0009](../docs/tracking/decisions/0009-채택-판정을-절대-축으로-통일한다.md)
에 있다. **읽고 시작하라 — 이 사이클 하네스 계약이 바뀐 지점이다.**

### 바뀐 계약 (되돌리지 마라)

1. **채택 판정은 전 전략이 코사인 절대 축 하나를 쓴다.** 순위 품질(recall@k)은 컷과 분리해
   전체 순위로 계산한다. 리랭크 행도 풀 밖 조항을 융합 순서로 이어 전체 순위를 복원한다.
2. **`fusion_pool_size`(기본 10) > `rerank_top_n`(기본 5)을 코드가 강제한다.** 같거나 작으면
   리랭크가 채택 집합을 바꿀 수 없어 실행 전에 거부된다.
3. **RRF 컷 기본값은 0.0(무필터)이고, 0 이 아닌 값은 이 코퍼스에서 실제로 걸러낼 수 있는지
   검사한 뒤에만 받는다.** 도달 불가능한 값은 최소 도달 가능 수치를 담아 거부한다.
   (구 기본값 0.02 는 조항 26개·rrf_k 60 에서 최소 RRF 0.023256 아래여서 항등이었다.)
4. **컷 스윕은 전략마다 리포트에 실린다.** 검색을 다시 돌리지 않으므로 추가 과금이 없다.
   비교표에 precision·recall 분모를 함께 인쇄한다 — 분모 없이 전략 간 precision 을 비교하면
   서로 다른 표본의 평균을 나란히 놓는 것이다.
5. **재작성은 벡터 다리·BM25 어휘 다리·리랭크 입력에 모두 적용된다**(원문은 계속 함께 유지).
   하드 게이트 4 의 "조용한 폴백 금지"가 이제 실제로 구현됐다 — 폴백 건수·사유·토큰이
   리포트에 집계된다.
6. **과금 축이 둘이다.** `paid_rerank` 가 `live` 와 별개 인자다. 미승인 상태의 리랭크 단은
   대역으로 메우지 않고 "미측정 + 사유"로 남는다. `--bge-m3` 는 이제 OpenAI 를 부르지 않는다.
7. **재작성 조건은 픽스처 경로와 코드로 결속되고 기본값이 없다.** 저장소 픽스처 경로가 조건을
   결정하며 선언이 다르면 거부한다. 저장소 밖 입력은 조건 명시를 요구한다.
8. **`load_retrieval_labels` 는 호출자가 실제로 쓸 골든셋·정책 디렉터리로 교차검증한다.**
   구 코드는 기본 경로로만 검사해 `--policy-dir` 에서 fail-open 이었다.
9. **하드 게이트 5·6 의 보호 범위에 정답 라벨이 들어왔다.** 런타임 격리 구조 테스트가
   검사 대상 모듈을 패키지 디렉터리에서 **유도**하고(하드코딩 목록 폐기),
   `retrieval_labels.jsonl`·`load_retrieval_labels` 도 금지 목록에 넣고, f-string·동적
   import 우회를 잡는다. 새 런타임 모듈이 조용히 검사 밖으로 나가지 않는다.
10. **실패 귀인 분류가 다섯이다.** `generation_issue`(정답 조항 **전부** 채택) /
    `partial_retrieval_failure`(일부만) / `retrieval_failure`(전부 누락) /
    `answered_without_relevant_evidence`(근거 없이 게이트 통과 — 이 제품에서 가장 위험한 실패) /
    `expected_no_answer`. 구 코드는 일부만 찾아도 생성 문제로 셌고, 근거 없이 답변이 확정된
    케이스는 아예 세지 않았다.
11. `DEFAULT_RERANK_MODEL` 은 `config.py` 의 `rerank_model` 로 옮겼다. BGE-M3 는 리비전을
    핀했다(`BgeM3EmbeddingClient.REVISION`). 리랭크 캐시 키에 지시문이 들어간다.
12. 산출물 이름에 재작성 조건·컷·top_k 가 들어간다:
    `retrieval-strategies-<모드>-<모델>-d<차원>-<blind|oracle>-k<top_k>-c<컷>`.
    **이것은 "리포트 이름 계열을 늘리지 않기로 한 판단"을 뒤집은 것이 아니다** — 계열은
    그대로 하나이고 같은 계열 안에서 실행 조건을 식별 가능하게 만든 것이다. blind 산출물과
    oracle 산출물이 접미사 숫자로만 갈리면 결정 기록이 인용할 파일을 이름으로 특정할 수 없다.

### 검증 상태

```
ruff check .          All checks passed!
ruff format --check   94 files already formatted
mypy (strict)         no issues in 48 source files
pytest                692 passed          (수정 전 662)
pytest -m db          146 passed, skip 0
```

대역 실행으로 리포트 생성까지 확인했다(과금 0회, DB 0회):
`uv run python -m scripts.compare_retrieval --stub-embedding --dimensions 64 --out-dir <임시>`

**핵심 수정은 실제 30건 데이터로 확인했다.** 컷 0.55 에서 빈 정답 5건(G21–G24·G28)이
네 전략에서 **동일하게 기권**한다(G22 만 1건 채택 — 산술이 아니라 실제 검색 문제).
수정 전에는 하이브리드·리랭크가 구조적으로 5건을 채택해 precision 0 이었다. 이 성질은
테스트로 못박혀 있다: `test_하이브리드와_리랭크도_절대_게이트로_기권한다`,
`test_도달_불가능한_RRF_컷은_거부된다`,
`test_리랭크_후보_풀이_채택_상한보다_작으면_거부된다`,
`test_리랭크는_후보_풀에서_골라_하이브리드와_다른_집합을_채택할_수_있다`.

### 작업 트리 상태 — **커밋 완료** (`6fe899c`)

수정 21개 파일 + 신규 결정 문서 1개, 약 +1,768 / −326 줄.

- 소스: `retrieval_eval.py`(대폭) · `retrieval_strategies.py` · `retrieval_labels.py` ·
  `evaluation.py` · `llm.py` · `config.py` · `contracts.py` · `scripts/compare_retrieval.py`
- 테스트 6개 파일 (신규 케이스 다수)
- 문서: `docs/tracking/decisions/0009-*.md`(신규) · `decisions/index.md` ·
  `findings.md`(12·13번 추가) · `status.md` · `operations.md`(9단계 신설) ·
  `engineering-notes.md`(뚫린 경로 4건) · `scripts/AGENTS.md` · `src/reply_gate/AGENTS.md`

**하네스 수정 자체는 `data/` 를 건드리지 않았다.** 같은 커밋에 들어간 `data/` 3종
(`retrieval_labels.jsonl` · `rewritten_queries.jsonl` · `rewritten_queries_oracle.jsonl`)
변경은 아래 "입력 3종 결정 완료" 절의 결정 0010 확정분이다 — 두 갈래를 한 커밋으로 묶었다.

### 다음 세션이 해야 할 것 (순서) — **1·2 완료, 3 이 다음 차례**

1. ~~커밋 지시를 받으면 커밋한다~~ → `6fe899c` (브랜치 `dryforge/retrieval-quality`).
2. ~~T6 실행 지시를 별도로 받는다~~ → 지시를 받아 실행했고 `121f284` 로 커밋됐다.
   실행 조건과 수치는 아래 "T6 실측 완료" 절.
3. **T7·T8(제품 배선)이 다음이다.** 이때 **하드 게이트 9 의 격리 검사가
   `retrieval_strategies.py` 를 이미 덮고 있다** — 런타임 배선이 픽스처를 읽으면 빨개진다.

## 진행 상태 (2026-08-13 후반) — 입력 3종 결정 완료, T6 앞이 열렸다

**사용자가 인터뷰로 4건을 확정했다.** 근거·기각한 대안은
[결정 0010](../docs/tracking/decisions/0010-검색-정답의-관련성-경계를-정하고-blind-재작성을-모델이-만든다.md).
findings 12·13 은 **종결**됐다.

1. **정답 관련성 경계 = 정면 조항 + 상충 조항.** G16·G17 에서 4-2 를 빼고(→ 4-1 하나),
   G18 에 상충 조항 2-4 를 넣었다. 조건·제한 조항(3-4·1-2)과 채널·전제 조항은 넣지 않는다.
   **좁아진 케이스와 넓어진 케이스가 함께 있다** — 한 방향으로만 움직였으면 지표를 좋아
   보이게 하는 변경과 구분되지 않는다.
2. **실행 요청은 현행 유지 + 규칙 명문화.** 정답 유무는 실행 요청인지가 아니라 "조항이 그
   요청에 답하거나 스스로 할 방법을 주는가"로 가른다. G25 는 정답 있음, G28 은 빈 정답.
   **기권이 정답인 5건이 유지된다.**
3. **oracle 2건을 라벨에 맞췄다.** G28 은 원문 유지, G25 는 주문번호 어휘 제거.
   "oracle ≥ blind" 전제 회복.
4. **blind 재작성 30건을 실제 모델로 재생성했다.** `scripts/generate_blind_rewrites.py`(신규)
   가 런타임과 같은 등급 모델에 문의 원문만 준다. 유일한 유료 호출이었다.

### 리뷰어 3:1 분쟁의 결말 — 지적한 현상은 실재하고, 원인 진단이 틀렸다

모델이 만든 blind 가 사람이 쓴 것보다 정답 조항 어휘를 **더 많이** 들여온다.

| 픽스처 | 유입 케이스 | 유입 2-gram |
|---|---:|---:|
| 구 blind (사람) | 7 / 25 | 16 |
| 신 blind (모델) | **18 / 25** | **33** |
| oracle | 25 / 25 | 144 |

정책 코퍼스를 입력으로 받은 적 없는 모델이 같은 어휘를 재현하므로, 겹침은 누출이 아니라
구어체→문서체 정규화의 결과다. 이제 **정답 조항에만 있는 값**(근거 ID·조항 번호·정책 고유
수치)만 구조 테스트가 막는다.

### 남은 것 하나 — **종결** (T6 산출에 병기됨)

같은 검색 결과를 구·신 라벨로 각각 채점해 실었다(추가 과금 0). **신 라벨이 네 전략
모두에서 F1 을 낮춘다** — 0.650→0.634 · 0.756→0.734 · 0.765→0.744 · 0.773→0.752.
지표를 좋아 보이게 만든 라벨 변경이 아니라는 것이 수치로 남았다(결정 0008 때와 같은 규율).

## 진행 상태 (2026-08-13 심야) — **T6 실측 완료, 다음은 T7** (커밋 `121f284` · `1eacded`)

**과금선을 넘었다.** 사용자 실행 지시를 받아 격자 비교를 돌리고 커밋했다.
**이 절은 기록이다** — 그 시점에는 작업 트리가 clean 이었다. 지금 상태는 아래
"2026-08-14 새벽" 절이 들고 있다(미커밋 작업 있음).

### 산출물 — `reports/retrieval-strategies-live-*` 5쌍 10개 파일 (git 추적)

| 접미사 | 조건 |
|---|---|
| `text-embedding-3-small-d1536-blind-k5-c030` | 기본 비교 (재실행 1회분이 `-2` 로 함께 있다) |
| `text-embedding-3-small-d1536-oracle-k5-c030` | 상한 대조 |
| `text-embedding-3-large-d1536` · `-d3072-blind-k5-c030` | 임베딩 모델 축 |

**하드 게이트 7 대로 기존 라이브 실측 12개 파일은 손대지 않았다.**

### 수치 (text-embedding-3-small 1536, 각 전략 자기 최적 컷)

| 전략 | best cut | precision | recall | macro F1 | r@1 (컷 0.30) |
|---|---:|---:|---:|---:|---:|
| vector | 0.50 | 0.628 | 0.640 | 0.634 | 0.5600 |
| + rewrite | 0.55 | 0.652 | 0.840 | 0.734 | 0.7600 |
| + hybrid (BM25+RRF) | 0.55 | 0.668 | 0.840 | 0.744 | 0.6400 |
| + LLM rerank | 0.55 | 0.680 | 0.840 | 0.752 | 0.8400 |

- **이득의 83% 가 재작성 한 단에서 나온다**(F1 +0.100). 하이브리드 +0.010, 리랭크 +0.008.
- **하이브리드는 F1 을 조금 올리면서 r@1 을 떨어뜨린다**(0.76 → 0.64). RRF 가 정답 조항을
  1위에서 밀어내고 리랭크가 되돌린다(→ 0.84). 세 임베딩 모델 전부 같은 방향이다 —
  하이브리드를 채택한다면 **리랭크와 함께**여야 한다.
- **표적 케이스 G17 이 재작성 단에서 닫힌다** — `policy:support:4-1` 0.264(3위, 컷 아래)
  → **0.580(1위)**. 하이브리드가 다시 3위로 내리고 리랭크도 이 케이스는 복구하지 못한다.
- **blind 0.734 대 oracle 0.853** — 배포 가능한 재작성이 상한의 약 86% 를 가져온다.
  결정 0010 이 회복시킨 "oracle ≥ blind" 전제가 전 구간에서 확인됐다.
- **모델 상향은 벡터 단독에서만 크다**(0.634 → 3-large 1536 0.670 · 3072 0.674).
  사다리 끝에서는 차이가 거의 사라지고(0.752 / 0.764 / 0.759) **3072차원이 1536보다
  낫지도 않다.** 상향은 볼륨 재생성(T8)을 동반하므로 재작성을 먼저 배선하는 쪽이 싸다.
  BGE-M3 행은 로컬 의존성 미설치로 **미측정**이다.
- **기권**: 빈 정답 5건 중 G21–G24 는 네 전략 모두 자기 최적 컷에서 0건 채택.
  G28 만 채택이 남고(벡터 5건 → 재작성 3건), 재작성이 과채택을 오히려 줄인다.

### 실행 중 고친 것

`--embedding-axis` 가 `--live` 를 무시해 **리랭크 단이 조용히 3단으로 잘렸다.** 축 경로가
비축 경로와 다른 식을 써서 `--rerank-with-openai` 의 "`--live` 는 이미 포함한다"는 안내가
거짓이었다. 같은 식으로 통일하고 테스트로 못박았다.

### 검증 상태

```
ruff check . / format --check / mypy(strict)   클린
pytest                                          694 passed   (T6 전 692)
pytest -m db                                    146 passed, skip 0
```

### 열려 있는 것 (T7 앞)

1. ~~**채택 구성이 아직 결정되지 않았다.**~~ → **2026-08-14 인터뷰로 확정됐다.**
   재작성만 채택 · 하이브리드/리랭크/모델 상향 미채택 · 컷 0.50.
   근거와 함정은 아래 "2026-08-14 새벽" 절의 "T7 채택 범위".
2. **findings 14 신설** — 리랭크 캐시가 한 번 미스했고 재현되지 않는다. 라벨 누출은
   세 갈래로 배제했다(입력 구성 · 순위 0/30 차이 · 캐시 항목 공유).
3. **검색이 좋아지면 L2 라이브 기각 장면이 줄어든다.** G21–G24 가 검색에서 걸러지면
   L2 재현은 G18(모순 기각) 하나가 맡는다 — 이 장면을 살리려고 검색을 일부러 느슨하게
   두지 마라(아래 "작성 중 확정됐지만…" 절).

## 진행 상태 (2026-08-14 새벽) — **사이클 1·2 코어 적대리뷰 + 치명 수정 완료** (`b211f54`·`a2fd32e`)

### 다음 세션의 **첫 행동 하나**

**아래 "2차 묶음" 6건 중 사용자가 고른 것부터 착수한다.** 고르지 않았으면 오탐 1건
(답변에 나가지 않는 키의 PII 로 기각 — `AGENTS.md` 불변식 7 위반)부터 간다: 헤드라인
지표가 "정상 초안 오탐률"이라 지표를 직접 오염시키는 유일한 항목이다.
**T7(재작성 배선 + 컷 0.50)로 넘어가기 전에 정리한다** — 게이트가 흔들리는 상태에서
검색을 바꾸면 어느 쪽이 수치를 움직였는지 가릴 수 없다.

작업 트리는 clean 이다.

### 왜 이 리뷰를 돌렸나

**사용자가 사이클 1·2(제품 코어)에 대한 Codex 블라인드 적대리뷰를 지시했다.** 이유는
사이클 3 리뷰가 값을 한 이유가 *설계*가 아니라 *리뷰*였기 때문이다 — 설계 모델은 사이클
내내 fable 로 상수였고, 달랐던 것은 멀티 적대리뷰뿐이었다. 사이클 1·2 코어(5,658줄)는
그 조건으로 검사된 적이 **한 번도 없었다.**

**4슬라이스 × 3회 = 12회를 돌렸고 10회 완주했다.** A(보안·데이터) 2회는 Codex 제공자의
사이버보안 콘텐츠 필터에 걸려 죽었다(로그에 요지는 남았다). 리뷰 산출물 원문은
`$CLAUDE_JOB_DIR` 밖 임시 디렉터리에 있어 **세션이 끝나면 사라진다** — 아래 요약이 전부다.

### 필터로 죽은 2회는 회수했다 — **재실행 방법이 이 절의 핵심 자산이다**

같은 프롬프트로 다시 돌리면 또 죽는다. 두 가지를 바꿔야 통과했다.

1. **산출 형태**: 등급(`FATAL`)·치명 4경로 목록·공격 서술("우회한다", payload 문장)을 빼고,
   **"문서가 주장하는 불변식 중 코드에서 성립하지 않는 것을 찾아 실패하는 pytest 함수로
   표현하라"** 로 재구성한다. 발견 하나당 `파일:줄` / 어긋난 불변식 / **지금 돌리면 실패하는
   테스트 함수** / 고칠 방향. 등급 대신 **불변식의 출처(문서 경로:줄)** 를 적게 한다.
   이건 타협이 아니라 **더 나은 형식이다** — 어차피 손으로 재현 코드로 옮겨 검증하게 된다.
2. **슬라이스 분할**: 한 응답에 모이는 양을 줄인다(`sql_guard` 단독 / `evidence`+`records`+`db`).

이 형식으로 재실행한 2회는 둘 다 완주했고, **진행 중이던 수정본의 구멍까지 찾아냈다.**

### 치명 결함 4건 — 전부 재현 확인 후 수정 완료 (`b211f54`)

1. **SQL 원문 → `evidence_text` → PII allowlist 세탁.** SQL 문자열은 문의의 영향을 받는
   LLM 산출이라 **문의로 유도 가능**했다.
2. **출력 별칭 이름이 같은 구멍을 한 겹 아래에서 열었다.** 1번을 이름 기반 provenance 로
   막았는데 출력 이름도 LLM 이 정한다 — `SELECT status AS "010-9999-9999"` 는 별칭이 결과
   dict 의 **키**가 되어 `key=value` 로 렌더되며 다시 allowlist 에 올랐다.
   **이 구멍은 재실행한 리뷰가 찾았다** — 필터에 죽은 채로 뒀으면 수정본을 검사할 사람이 없었다.
3. **PII 표기 변형 미탐 6종** — `+82`/`0082`, 괄호·공백 구분자, 공백 주민등록번호, 국제화
   도메인 이메일, 전각 숫자, 폭 없는 서식문자 삽입.
4. **빈/공백 `claim.text` 통과** → `answer=""` 로 `answered` 종결.

**공격 14건 차단 · 양성 대조 5건 통과**를 실행해 확인했다. 특히 근거는 반각인데 초안이
전각으로 쓴 경우가 통과한다 — 접기를 **양쪽에** 걸었기 때문이다. 한쪽에만 걸면 정상 에코가
오기각돼 헤드라인 지표(오탐률)를 오염시킨다.

**⚠ L1 PII 검출률의 정의가 넓어졌다.** 사이클 1·2 라이브 수치와 직접 비교할 수 없다.

Codex 가 수정 중 다섯 번째를 스스로 찾았다(engineering-notes 에 기록): allowlist 가 부분
문자열 대조라, 근거의 긴 숫자(주문번호) 안에 지어낸 번호가 우연히 포함되면 통과한다.

### 인계 게이트 배선도 함께 들어왔다 (`a2fd32e`)

Codex 가 수정 지시 범위 밖에서 `.claude/settings.json`·`.codex/config.toml` 을 건드렸다.
**되돌리려다 확인해 보니 되돌리면 안 되는 것이었다** — `~/.claude/hooks/handoff/` 의 실존
게이트를 Codex 세션에도 거는 배선이고, 커밋 `d984bce`(라이브 리포트 보호를 Codex 에도)가
만든 선례와 같은 모양이다. 보안 수정과는 별개 사안이라 커밋만 갈랐다.

### 아직 안 고친 것 — 2차 묶음 (사용자가 순서를 정한다)

- **오탐: 답변에 나가지 않는 키의 PII 로 기각된다.** `{"claims":[...정상...],"debug":"1588-1234"}`
  → `reject[pii_detected]` 인데 `debug` 는 `to_draft` 가 버려 답변에 없다.
  **`src/reply_gate/AGENTS.md` 불변식 7 을 코드가 위반한다.** 헤드라인 지표가 "정상 초안
  오탐률"이라 지표 오염이다. `b211f54` 뒤에도 **재현된다**(직접 확인함) — 첫 착수 후보다.
- **L2 호출 실패가 attempt `pass` 로 기록된다**(`pipeline.py` `_judge_failure` 가
  `l2_expected=False` 로 결합). 미판정이 통과로 세어진다. 리뷰어 4명 수렴.
- **동일 텍스트 claim 2건이 L2 판정 1건으로 접힌다**(`judge.py` 가 집합 대조). 리뷰어 5명 수렴.
- **주입 SDK 재시도가 래퍼 재시도와 곱해진다** — 실제 전송 최대 6회인데 `attempts=2`.
  C 슬라이스 3명 전원. `AGENTS.md` 의 "두 SDK 모두 `max_retries=0` 을 풀지 않는다"가
  **주입 경로에서 성립하지 않는다.**
- **`policy_index` 에 임베딩 provenance 가 없다** — 모델을 바꾸고 재색인하지 않으면 다른
  벡터 공간을 비교하면서 정상 수치를 낸다. T8 과 직결된다.
- **`api.py` 가 body 검증·판정 키 선검사보다 DB 연결을 먼저 연다** — 422·503 이 500 에 가린다.
- **`GROUP BY` 별칭이 시스템 컬럼 검사를 가린다.** `SELECT ctid` 는 `unknown_column` 으로
  거부되는데 `SELECT count(*) AS ctid ... GROUP BY ctid` 는 통과한다. engineering-notes 의
  "별칭이 컬럼 화이트리스트를 무력화했다"가 **`HAVING` 은 제외한다**고 명시하며 고친 계열인데
  `GROUP BY` 가 남았다 — Postgres 는 둘 다 맨 이름을 입력 컬럼으로 먼저 해석한다.
  **다만 이 경로로 시스템 컬럼 값이 출력되는 쿼리는 만들지 못했다** — 심층 방어 침식이지
  활성 유출이 아니다. 등급을 올리지 마라.
- **같은 `WITH` 안 중복 CTE 이름이 가드를 통과한다.** 다만 PostgreSQL 이 그 쿼리 자체를
  거부하므로 데이터는 새지 않는다. fail-closed 원칙 위반이지 유출 경로가 아니다.

### 사용자 판단 사안 (코드 수정이 아니다)

- **목표 75% 가 새 라벨에선 L2 없이도 달성된다**(L2-off 기준선 86.7%). D 슬라이스 3명 전원.
  `evaluation.TARGETS` 주석에 폐기된 전제가 그대로 남아 있다. 결정 0006 갱신은 T11 이다.
- **결정 0007 의 "배치만이 모순 감지를 보존한다"가 false dichotomy** — claim별 support 호출
  + 전체 근거 contradiction 호출로 나누는 hybrid 를 검토하지 않았다. J11 이 그 비용이다.
- **J11 라벨 자체가 틀렸을 수 있다** — C 슬라이스 3명이 독립적으로 "환불 7일 vs 반품 14일이
  같은 사안인지 입력에 명시되지 않는다"고 지적했다. 판정기가 옳고 픽스처가 틀렸을 가능성.
- **`bait_reject_recall` 이름이 과장이다** — 분자가 `rejected_at_least_once`(층·사유 무관)라
  26.7% 는 미끼 재현율이 아니라 "reject_bait 범주의 any-layer 기각률"이다.
  findings 1번이 "`pii_detected` 는 0/15"라고 이미 정직하게 적어 두긴 했다.

### T7 채택 범위 — **사용자 인터뷰로 확정됨 (2026-08-14)**

- **재작성 채택.** 이득의 83%(F1 +0.100), G17 표적 케이스를 닫는다(4-1 유사도 0.264→0.5795).
- **하이브리드·리랭크 미채택.** 조항 26개 코퍼스에는 순위 문제가 없다 —
  **벡터 단독이 이미 r@5 = 1.000** 이다. 두 단은 재정렬만 하므로 이미 풀린 문제를 공격한다.
  하이브리드는 r@3 을 1.000 → 0.920 으로 **악화**시킨다.
- **임베딩 모델 상향 미채택.** 사다리 끝에서 0.752 / 0.764 / 0.759 로 차이가 없고 3072차원이
  1536 보다 낫지도 않다. **T8(볼륨 재생성)이 불필요해진다.**
- **채택 컷 0.30 → 0.50.** 무근거 4건이 전부 기권하는 **가장 낮은** 컷이다(findings 2 종결).
  F1 최적점은 0.55 지만 recall 을 0.920→0.840 으로 깎고 G17 마진이 0.08→**0.03** 이 된다.

**⚠ 함정 — 컷을 단독으로 바꾸지 마라.** 0.50 은 재작성이 유사도를 밀어 올린 뒤의 값이다.
재작성 없이 컷만 올리면 **recall 이 0.880 → 0.640 으로 무너진다.**
`config.vector_similarity_threshold` 변경은 **재작성 배선과 같은 커밋**으로만 나간다.

### 사이클 1·2 라이브 수치 — **구 설정 기준선으로 강등** (사용자 확정)

산출물은 사후 편집하지 않는다(findings 9 원칙). `status.md` 에 **"구 L1 정의 · 컷 0.30 기준"**
이라고 명시하고, L2 기여도 분해는 새 계열에서 다시 쌓는다. 치명 수정이 L1 의 PII 탐지 정의를
넓히므로 **컷과 무관하게 이미 재측정 대상**이다 — "비교 보존"은 더 이상 컷 유지의 논거가 아니다.

### 이 리뷰가 남긴 방법론 (사이클 4 로 가져갈 것)

- **적대리뷰를 상설 게이트로 올려라.** 사이클 3 의 완성도 차이는 설계가 아니라 리뷰에서 왔다.
  안 돌리면 사이클 4 는 같은 자리로 돌아간다.
- **리뷰의 수확은 버그가 아니라 "숫자가 라벨이 말하는 것을 뜻하지 않는다"였다.** 이 계열은
  테스트로도 라이브로도 안 잡힌다 — 전부 녹색이고 전부 숫자를 낸다.
- **이미 관대하게 설명해 둔 이상 신호를 공격 대상으로 지목하라.** `pii 0/15` 와 `J11` 을
  루브릭에 박았더니 리뷰어 7명이 독립적으로 그 설명을 깼다.
- **보안 슬라이스는 Codex 제공자 필터에 걸릴 수 있다.** 12회 중 2회가 그렇게 죽었다.
  재시도하거나 Claude 서브에이전트로 그 슬라이스만 돌리는 우회가 필요하다.

## 하드 게이트 (실행 에이전트가 코드에서 유도할 수 없는 불가침)

1. **게이트 두 층은 무변경이다.** `gate.py`·`judge.py` 의 동작, 사유 4종·2종, L1 의
   LLM·네트워크 import 금지, L2 의 배치·fail-closed. 이번 사이클은 **게이트 앞**만
   손댄다. 검색이 좋아졌다고 판정을 느슨하게 하지 마라.

2. **재작성 질의 픽스처는 런타임으로 새면 안 된다.** `data/rewritten_queries.jsonl` 은
   오프라인 비교 전용이다. 실행 경로가 읽으면 골든셋 30건 전용 치트가 되고 측정 2 의
   수치가 통째로 무의미해진다. **구조 테스트로 못박고, 그 테스트가 실제로 실패하는지
   확인하라**(참조를 일부러 넣어 빨개지는 것을 본 뒤 되돌린다).

3. **검색 단계 실패는 폴백이고, 배선 누락은 조립 오류다.** 둘을 섞지 마라.
   런타임 호출 실패(재작성·리랭크) → 폴백해서 계속. 전략 켜짐 + 클라이언트 미배선 →
   조립 시점에 명시적으로 죽는다. **L2 호출 실패가 인계인 것은 변함없다.**

4. **조용한 폴백 금지 · 토큰 은폐 금지.** 폴백은 처리 기록과 리포트에 남는다. 실패한
   호출의 토큰도 실비용이므로 센다. 검색 단계 생성 토큰은 생성·임베딩·판정 어디에도
   합산하지 않는다.

5. **평가 장치 제거 금지 — 목록이 둘 늘어난다.** 기존(정책 문서의 미끼·모호·상충 조항,
   골든셋, `l1_fixtures.jsonl`, `judge_fixtures.jsonl`)에 더해
   **`data/retrieval_labels.jsonl`** 과 **`data/rewritten_queries.jsonl`** 이 같은
   자격으로 들어온다. 라벨이 사라지면 이번 사이클 모든 수치의 분모가 사라진다.

6. **라벨은 채점자만 본다.** 어떤 검색 전략도 정답 라벨을 입력으로 받지 않는다.
   비교 하네스 안에서 둘이 가까이 있어 실수로 새기 쉽다 — 테스트로 지켜라.

7. **산출물 보존.** 기존 라이브 실측 6회분 **12개 파일**(`reports/evaluation-live-{1,2,3}`
   · `evaluation-live-l2-{1,2,3}` × `.md`·`.json`)은 덮어쓰지도 지우지도 않는다.
   새 검색 비교 리포트(`reports/retrieval-*`)는 git 추적 대상이고, 추적되는 순간
   저장소의 PreToolUse 훅이 Edit/Write 경로의 사후 편집을 막는다. **훅이 못 잡는 경로가
   하나 있다** — 비교 스크립트 재실행이다. 그래서 하네스가 기존 산출물에 쓰기를 스스로
   거부해야 한다(spec §14).

8. **과금은 명시적으로만.** 상한 $10 안에서 오프라인 비교·재색인은 승인 없이 돈다.
   **라이브 재실측(T10)은 실행 직전에 한 번 더 확인받는다.** 상한을 넘길 상황이면
   실행 전에 멈추고 보고한다. 비밀은 `.env` 에만 — 로그·리포트·오류 메시지에 값 금지.

9. **볼륨 재생성 전 사용자 확인 + 처리 기록 백업·복원.** 차원 변경이나 스키마 확장이
   채택에 포함되면 T8 의 절차를 그대로 탄다. 복원은 **API 로 실제 열어** 확인한다 —
   건수 대조만으로는 부족하다.

10. **미달이면 원복이 기본이다.** 라이브가 오프라인 채택을 반증하면 기존 기본값으로
    되돌리고 실측·원인을 남긴다. **실측이 정당화하지 않는 기본값을 저장소에 남기지
    않는다** — 남기면 "측정해보니 충분해서"라는 채택 근거 전체가 무너진다.

11. **미실행·미측정을 0 으로 채우지 않는다.** BGE-M3 미설치, 라벨 부재, 중단 — 전부
    "미실행 + 사유"다. 기존 리포트 불변식 그대로다.

12. **검증 게이트**(병합 전): `ruff check` · `ruff format --check` · `mypy`(strict) ·
    `pytest` 전체 녹색 + `pytest -m db` **skip 0**. DB 를 재생성했다면 재생성 상태에서
    다시 확인한다.

13. **`.dryforge/` 는 이 저장소에서 버전관리 대상이다 — gitignore 하지 마라.**
    **이 3-doc 은 이미 커밋돼 있다(`da4c6a1`).** 프로듀서가 untracked 로 넘기는 것이
    일반적인 인계 상태지만 여기서는 아니다 — 추적 상태가 정상이고, 더러운 작업 트리도
    이상 징후도 아니다. 이 사실 때문에 멈추지 마라.
    `.gitignore` 는 `.dryforge/worktrees/` 와 `.dryforge/backup/` 만 무시하고, 그 자리에
    "3-doc 본문은 버전관리 대상"이라고 적혀 있다. 사이클 1·2 의 3-doc 이 실제로
    `.dryforge/001/`·`002/` 로 커밋돼 있다.
    - 이번 3-doc 은 사이클 끝에 **`.dryforge/003/` 로 옮겨 커밋**한다(앞 두 사이클과 같은
      방식). 작업 중에는 루트의 `spec.md`·`plan.md`·`handoff.md` 그대로 둔다.
    - **`git rm -r --cached .dryforge/` 를 실행하지 마라.** 이전 사이클이 남긴 추적 상태를
      정리하는 기본 절차지만, 여기서는 **보존 대상인 001·002 를 통째로 추적 해제한다.**
    - 이 항목은 실행 하네스와 무관하다(Claude Code · Codex 어느 쪽이든 같다).

14. **이 사이클은 설계와 실행이 다른 세션·다른 하네스에서 돈다.** 설계 대화는 넘어오지
    않는다 — **이 3-doc 이 전부다.** 그래서 판단이 갈리는 지점을 이 문서에 박아 뒀다.
    문서로 풀리지 않는 것이 나오면 **추측하지 말고 사용자에게 올려라.** 설계 세션에
    물어볼 수 없다는 사실이 escalate-don't-guess 를 더 엄격하게 만든다, 느슨하게가 아니라.
    - Codex 세션에서도 `reports/` 아래 커밋된 산출물은 훅이 막는다
      (`.codex/config.toml` → `.claude/hooks/protect-live-reports.sh codex`).
      막히면 우회하지 말고 그 사실을 보고하라 — 그게 훅의 목적이다.

## 작성 중 확정됐지만 spec/plan 에 자리가 없는 의도

- **이 사이클의 판정 기준은 "측정해서 보여주기"가 아니라 "실제로 좋아지기까지"다.**
  사용자가 그렇게 골랐다. 비교표만 만들고 채택 없이 끝내지 마라. 다만 **어떤 단도
  표적 결함을 개선하지 못하면 기본값을 바꾸지 않는 것**이 올바른 결말이고, 그때는
  그 사실이 이 사이클의 산출이다 — 억지로 채택하지 마라.

- **채택 규칙을 목표선으로 바꾸지 마라.** "recall@5 ≥ 0.9" 같은 경계는 명시적으로
  기각된 선택지다(결정 0006 의 논리 — 측정 전에 목표를 적으면 숫자를 맞추러 간다).
  spec §7 은 경계선이 아니라 **직전 단과의 상대 비교 규칙**이고, 그래서 실측 전에
  정할 수 있다.

- **리랭커가 생성기와 같은 계열(OpenAI)인 것은 알고 고른 것이다.** 판정 계열 분리
  (결정 0004)는 self-judging bias 를 막는 규칙이고, 리랭크는 자기 출력을 평가하는 것이
  아니라 문서를 고르는 일이다. 다만 "생성기와 같은 계열이 재료를 고른다"는 편향
  가능성은 남으므로 **리랭크 없는 구성이 비교표에 함께 있어야 한다** — 그 차이가 곧
  관측이다. 일관성을 이유로 리랭커를 Anthropic 으로 옮기지 마라(판정 계열에 검색
  책임을 얹으면 두 역할이 섞인다).

- **BM25 토큰화를 형태소 분석기로 "개선"하지 마라.** 문자 2-gram 은 의존성을 늘리지
  않으면서 한국어 조사 문제를 우회하는 선택이다. 26개 조항 규모에서 형태소 분석기의
  이득보다 의존성·설치 무게가 크다. 성능이 아쉬우면 그 사실을 비교표에 적는다.

- **비교를 DB 로 끌어들이지 마라.** 인메모리 계산은 편의가 아니라 설계다 — 차원이 다른
  벡터를 `vector(1536)` 에 담을 수 없고, 비교마다 볼륨을 재생성하면 보존하기로 한 처리
  기록이 실험마다 날아간다.

- **리포트 이름 계열을 늘리지 않기로 한 것은 판단이다.** L2 도입 때는 이름을 갈랐고
  (지표 정의 자체가 달라졌으므로) 이번엔 실행 조건 필드로 간다(같은 지표를 다른 설정으로
  잰 것이므로). 구성마다 이름을 늘리면 이름 가드가 조합 폭발한다. **이 판단이 틀렸다고
  보이면 임의로 바꾸지 말고 사용자에게 올려라** — 산출물 이름 규칙은 이 저장소가 한 번
  사고를 낸 자리다(`docs/engineering-notes.md`).

- **검색 개선이 성공하면 지금 가장 안정적인 L2 라이브 기각 장면이 사라진다.**
  무근거 4건(G21–G24)이 검색 단계에서 걸러지면 그 12/12 인계는 더 이상 L2 장면이
  아니다. 사용자는 **새 서사로 재구성**을 골랐다 — "검색이 먼저 거르고, 남은 의미
  결함은 L2 가 잡는다". L2 라이브 재현은 **G18(모순 기각, 사이클 2 에서 3/3)** 이
  맡는다. 이 장면을 살리려고 검색을 일부러 느슨하게 두지 마라.

- **성공 판정 ②를 "생성 토큰 0"으로 재지 마라.** 의도 해석 호출이 검색보다 먼저
  무조건 돌고 그 토큰이 `input_tokens`/`output_tokens` 에 들어간다 — 커밋된
  `reports/evaluation-live-l2-1.json` 의 G17 이 이미 `no_evidence` 인계인데
  `input_tokens 216` 이다. 판정 기준은 **시도(초안) 0건 + 판정 토큰 0** 이다(spec §10-2).
  "정직하게 0 으로 만들자"며 의도 해석 토큰을 새 검색 계열로 옮기지 마라 — 그건 생성
  토큰의 정의를 바꾸는 계약 변경이고 §8-5 의 무변경 목록에 있다.

- **G17 은 "검색이 성공하면 답변으로 끝나도 정답"이다.** 골든셋 라벨이 `answered` 와
  `no_evidence` 인계를 둘 다 허용한다(결정 0008). 표적 관측 ①은 **일치율이 아니라
  채택 근거에 `policy:support:4-1` 이 있는가**로 판정한다 — 그래서 T9 의 근거 ID 필드가
  T10 보다 먼저 서야 한다.

- **목표치 재확정은 이번 사이클 실측 뒤로 미뤄 둔 상태다.** 새 라벨 기준으로는 L2 꺼짐
  기준선(86.7%)도 목표 75% 를 넘어 목표가 이미 현황 추인이 됐다(결정 0008 위험 절).
  T11 에서 결정 0006 갱신으로 닫는다. **그 전에 임의로 숫자를 올리지 마라.**

- **`build_pipeline` 의 판정자 주입 구멍 문제(findings 10)를 이번에 고치지 마라.**
  검색 전략 배선이 파이프라인 조립을 만지게 되어 유혹이 생기는 자리다. 그건 공개 표면
  변경이고 별도 설계가 필요하다고 이미 판단해 둔 항목이다.

- **외부 SDK 규약은 구현 시점에 확인하라** — 임베딩 차원 파라미터(`3-large` 절단),
  저가 모델 ID, BGE-M3 로더 API. 기억으로 쓰지 말 것(프로젝트 규칙: "설정/CLI는
  실행해서 답한다").

- 사이클 1·2 인계 문서가 남긴 규율 그대로: **"고쳤다"는 자기 보고 대신 실제로 넣어보고
  확인한다.** 특히 재작성·리랭크 프롬프트는 표적 케이스(G17·G21–G24)를 실제로 흘려
  보고 조정하라(오프라인은 상한 안에서 자유).

## 완료의 정의

**게이트가 셋이다**(spec §15). 사이클 2 는 둘로 갈랐지만 이번엔 제품 배선이 **채택
결과에 종속**되어 배선 검증을 과금 앞으로 당길 수 없다.

- **(a) 비교 준비 완료 — 무과금.** T1–T5·T9. 검증 명령 전부 + 라벨·픽스처 로더 대조 +
  런타임 격리 구조 테스트 + 대역으로 비교 하네스 완주 + 검색/생성 분해 3분류.
- **(b) 채택과 제품 배선 — 오프라인 과금(상한 안).** T6·T7·T8. 폴백·미배선 테스트는
  **채택 구성이 검색 단계에서 생성 호출을 쓸 때만** 해당한다 — 안 쓰는 구성이 이기면
  "해당 없음"이지 미달이 아니다.
- **(c) 실측과 판정 — 라이브 과금(별도 승인).** T10·T11·T12.

표적 관측이 재현되지 않으면 무한정 붙잡지 말고 시도 내역과 수치를 그대로 보고해
사용자 판단을 받는다. **원복도 정상 결말이다.**
