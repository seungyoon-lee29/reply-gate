# 사이클 4 핸드오프 — 채택 축과 회귀 가드 (governing doc)

## 문서 역할

| 문서 | 역할 | 권위 |
|---|---|---|
| `.dryforge/spec.md` | **무엇을** — 행동 계약, ground truth | 충돌 시 이긴다. 오류는 사용자 승인으로만 수정 |
| `.dryforge/plan.md` | **어떻게·순서** — 태스크 13개와 실행 그래프 | 잠정. 자유롭게 수정 가능 |
| 기존 코드 | HOW 참고 + 현실 대조 | WHAT 의 권위가 아니다 |

프로젝트 컨텍스트는 `CLAUDE.md`/`AGENTS.md` + `docs/` 가 들고 있다.
**유지 규칙: 이 파일은 덧붙이는 곳이 아니라 현재 상태로 갈아 끼우는 곳이다.** 남기는 기준은
"다음 사람이 알아야 하는데 **코드도 git 도 결정 기록도** 말해주지 않는 것" 하나다.

## 지금 어디까지 왔는가 (2026-08-19) — **사이클 종료**

**브랜치 `dryforge/cycle4`, HEAD `efd8549`. 태스크 13개 + 최종 리뷰 반영까지 전부 끝났다.**

완료 검증(HEAD 기준 실행): `ruff check` 0 · `ruff format --check` 0 · `mypy` 0 ·
`pytest` **1056 passed** · `pytest -m db` **163 passed / skip 0**. 문서 링크·앵커는
프래그먼트까지 검사해 0건 깨짐. 서버 좀비 0.

각 태스크의 결과는 결정 기록 `0012`~`0020` 과 커밋된 리포트에 있다. **여기 다시 적지 않는다.**
이번 사이클에 산 라이브: `evaluation-live-l2-7` … `-27` (풀셋 6회 + 측정 3 단독 15회).

## 최종 리뷰 두 건 — 전부 처분됐다

코드 리뷰와 문서 리뷰 둘을 다 받았고 **다시 돌리지 않는다(둘 합쳐 65만 토큰)**. 처분:

| 갈래 | 건수 | 처분 | 결과가 있는 곳 |
|---|---|---|---|
| 코드 리뷰 blocking | 1 | 고침 | `abd5642` |
| 문서 리뷰 blocking | 6 | 전부 고침 | `efd8549` |
| 코드 리뷰 advisory | 22 | 고칠 것 7 고침 · 나머지 수용 기록 | `efd8549` · [findings 26](../../docs/tracking/findings.md) |
| 문서 리뷰 advisory | 14 | 사실·문면 오류 9 + `tests/AGENTS.md` 신설 · 구조 관찰 3 수용 기록 | 같음 |

**옮겨 간 것 셋** — 이 파일이 아니라 저장소가 들고 있다:

- 사이클 문서를 절 라벨로 인용하지 않는 규칙 → `docs/standards.md` "사이클 문서를 절
  라벨로 인용하지 않는다" + `tests/test_durable_citations.py` 의 `docs/` 층 가드.
- 테스트 관례(구조 검사 네 종류·음성 대조·`declared_settings()`) → **`tests/AGENTS.md` 신설**.
- 고치지 않기로 한 것의 사유와 재개 시점 → [findings 26](../../docs/tracking/findings.md).
  **1·2·4·7 은 승격 기준선 재등재와 한 묶음이다** — 실측 뒤 제품 동작을 바꾸면 이번
  사이클 라이브의 조건이 무효가 되기 때문이다.

## 사이클을 닫는 남은 단계

1. ~~리뷰 분류 → 반영 → 검증 재실행~~ — 끝났다(`efd8549`).
2. ~~사용자 인수 판정~~ — 받았다(2026-08-19).
3. **3-doc 을 `.dryforge/004/` 로 옮겨 커밋** — 이 커밋이 그것이다.
4. **워크트리 정리** — `a`(task/T11) · `b`(task/T13) · `c`(task/T8) 제거,
   `.dryforge/worktrees/` 삭제, 병합된 task 브랜치 8개(T5~T9·T11~T13) 삭제.
5. **main 병합** — 사용자가 "메인 병합"으로 정했다(2026-08-19). fast-forward 가능
   (main 대비 57 ahead / 0 behind).

## 다음 사이클로 넘어가는 미결

- **승격 기준선 재등재가 1순위다.** 현 기본값(재작성 · 컷 0.30 · 기권 게이트 τ=0.06 ·
  교정된 판정 픽스처)으로 풀셋 라이브를 돌린 세트가 **하나도 없다.** 재등재 전까지 구속
  줄은 영구히 "대조 불가"다. 풀셋 라이브 3회 구매 + 사람의 결정이고, 함께 정리할 다섯
  가지는 [findings 23](../../docs/tracking/findings.md) 에 적혀 있다.
  **findings 26 의 1·2·4·7 을 그 작업에 같이 실어라.**
- **OpenAI API 키 로테이션 권고.** 실패한 단언 하나가 pytest 출력에 키 전문을 실었다.
  저장소에는 안 들어갔지만 노출은 실제로 났다. 코드는 고쳤다(`96abb9e`·`be1f235`).
- **`chore/reply-gate-continuity-hooks`** — 사이클 3 부터 남은 미병합 가지
  (origin/main 대비 ahead 1 / behind 60). **처분은 사용자 몫이고 아직 안 났다.**
- **findings 19**(`missing_citation` → 재생성 → 통과 처리 기록)는 재현에 확률 층이 걸린
  과금 반복이라 열어 뒀다.

## 하드 게이트 (코드에서 유도할 수 없는 불가침)

`CLAUDE.md` "절대 깨지 않는 것" 7개와 `docs/standards.md` 가 정본이다. 그 밖에 이번
사이클에서만 사는 것:

1. **처분 고정**: judge 기본 모델 교체 금지 · 청킹 제품 반영 금지 · 비동기화 구현 금지.
   셋 다 "측정·검토 + 결정 기록"까지이고 전부 완료됐다(결정 0018·0015·0020).
2. **이번 사이클 API 응답 계약 확장 0.** 스키마·볼륨 무변경 사이클이다.
3. **과금은 명시적으로만.** 회당 상한 $10. 이번 사이클 남은 유료 실행은 없다.
4. **미달이면 원복이 기본** — 축별 원복(결정 0011). 이번 사이클에 두 번 실제로 발동했다
   (판정 프롬프트 강화·캐싱).

## 함정 (이번 사이클이 실제로 밟은 것)

- **τ 는 임베딩 모델에 묶인다.** `-3-small` 1536 에서만 분리했고 `-3-large` 계열은
  분리하지 못했다(여유 −0.0114 / −0.0160). 임베딩을 건드리면 게이트가 **조용히 무력해진다.**
  런타임에는 이걸 막는 장치가 없다 — 리포트 지문의 짝 하나가 전부다.
- **병렬 실행은 리포트 번호를 미리 갈라 줘야 한다.** 하네스 자동 넘버링은 각 워크트리가
  base 까지만 보므로 같은 번호를 고른다. `--report-stem` 으로 구간을 나눠 줘라.
- **리포트의 사람이 읽는 줄과 조건 지문이 갈릴 수 있다.** `-19`~`-24` 의 `conditions.judge`
  가 전부 `claude-sonnet-5` 로 잘못 적혔다. **정본은 `condition_fingerprint`** 다. 코드는
  고쳤고 산출물은 보존했다(결정 0018 "산출물 결함" 절).
- **설정 객체가 repr 되면 자격 증명이 따라간다.** 단언식 안에 `Settings(...)` 를 두지 마라 —
  bool 로 먼저 묶어라. Postgres DSN 경유 노출은 **검증하지 않았다.**
- **커밋된 라이브 리포트를 고치려고 다시 사지 마라.** 틀린 것이 사람이 읽는 줄이고 기계가
  읽는 값이 맞으면, 정정은 문서 쪽이다. 이번 사이클에 두 번 그렇게 처리했다 — `-19`~`-24`
  의 `conditions.judge` 오기와, `-10`~`-27` 18건의 *"짝인 `embedding_model` 도 함께
  달라졌다"*(임베딩은 안 바뀌었다). 둘 다 코드는 고치고 정정만 문서에 병기했다.
- **짝 지문을 적을 때는 짝이 실제로 달라졌는지 값을 봐라.** 짝 관계를 **설명**하는 문장과
  짝이 **달라졌다고 보고**하는 문장은 다르다. 위 18건이 그 구분을 안 해서 없던 사실을 실었다.
- **`--stub-llm` 이 DB 임베딩을 대역으로 덮어쓴 사고가 보고됐으나 기전을 재현하지 못했다.**
  코드상 stub 경로는 롤백을 건다. 라이브 전에 색인 출처를 확인하는 절차만 남겼다
  (`docs/engineering-notes.md` 가 "확인한 것 / 확인하지 못한 것"으로 갈라 적었다).
- **선언값을 잴 때 인자 없는 `Settings()` 를 쓰지 마라** — `.env` 와 프로세스 환경을 함께
  읽어 "기본값이 결정대로인가"가 로컬 환경 측정이 된다. `conftest.declared_settings()` 를
  쓴다(`tests/AGENTS.md` 불변식 8). 사이클 4 에서 실제로 그 상태였고 고쳤다.
- **링크 검사는 프래그먼트까지.** 앵커 무시 검사기가 "0건 깨짐"을 잘못 말한 전례가 있다.
- **확률 층 장면에 "항상"을 붙이지 마라.** "3/3"은 관측이지 결정론이 아니다.
- **설계 세션은 없다 — 이 3-doc 이 전부다.** 문서로 안 풀리면 추측하지 말고 사용자에게.
  소스 주석은 durable 문서만 인용한다(`tests/test_durable_citations.py` 가 막는다 — 이
  3-doc 을 소스에서 인용하지 마라).
