# 결정 기록

다른 팀이라면 다르게 골랐을 수 있는 선택만 적는다.

| 번호 | 제목 | 요지 |
|---|---|---|
| [0001](0001-게이트를-2층으로-나눈다.md) | 게이트를 결정론 층과 확률 층으로 나눈다 | 결정론 층의 지표가 재현되고, L2 는 스위치로 꺼서 L1 단독으로 돌릴 수 있다 |
| [0002](0002-PII를-근거-유래-allowlist로-판정한다.md) | PII 를 전면 차단이 아니라 근거 유래 allowlist 로 판정한다 | 정당한 연락처 에코를 살리면서 지어낸 값을 잡는다 |
| [0003](0003-에이전트-루프를-직접-구현한다.md) | 에이전트 루프를 프레임워크 없이 직접 구현한다 | 상한·분기·상태를 코드로 보이게 해 설명 가능성을 얻는다 |
| [0004](0004-생성-LLM을-OpenAI로-확정한다.md) | 생성 LLM 을 OpenAI 로 확정하고 상위 검증은 다른 계열로 남긴다 | 키 1개로 단순화하되 self-judging bias 를 구조로 막는다 |
| [0005](0005-SQL-조회-범위를-코드가-강제한다.md) | 생성된 SQL 의 조회 범위를 프롬프트가 아니라 코드가 강제한다 | 프롬프트 지시는 안전장치가 아니다 |
| [0006](0006-지표-목표치를-실측-뒤에-확정한다.md) | 지표 목표치를 실측 뒤에 확정하고, 일치율은 닿지 않는 값으로 둔다 | 목표를 실측에 맞춰 내리면 현황 기술이 된다 |
| [0007](0007-L2-판정을-배치-fail-closed로-짓는다.md) | L2 판정을 배치·fail-closed 로 짓고, "없다" 안내를 비대칭으로 판정한다 | 모순 감지가 교차 시야를 요구하고, 검증 못 한 답변은 내보내지 않는다 |
| [0008](0008-미끼-라벨을-판정-규칙에-정렬한다.md) | 미끼 라벨을 판정 규칙에 정렬한다 | 설계가 정답으로 정한 동작을 라벨이 불일치로 찍고 있었다 — 전후 수치 병기로 정직성을 지킨다 |
| [0009](0009-채택-판정을-절대-축으로-통일한다.md) | 검색 전략의 채택 판정을 하나의 절대 축으로 통일한다 | 순위 기반 점수로 채택을 판정하면 기권이 불가능해져 전략 간 precision 이 비교 불가가 된다 |
| [0010](0010-검색-정답의-관련성-경계를-정하고-blind-재작성을-모델이-만든다.md) | 검색 정답의 관련성 경계를 정하고 blind 재작성을 모델이 만든다 | 정면 조항 + 상충 조항까지가 정답이다. 사람이 쓴 blind 는 "정규화인가 누출인가"를 사람이 판정하게 만든다 |
| [0011](0011-원복을-축별로-가른다.md) | 라이브가 반증한 채택을 축별로 되돌린다 — 컷은 원복, 재작성은 유지 | "실측이 정당화하지 않는 기본값을 남기지 않는다"는 대칭이다. 정당화**하는** 축을 실측 없이 버리는 것도 같은 위반이다 |
| [0012](0012-적응-컷과-상대-마진을-반증하고-기권-게이트만-남긴다.md) | 적응 컷·상대 마진을 반증하고 후보를 질의 단위 기권 게이트 하나로 줄인다 | 상대 축은 1위를 지우지 못해 절대 하한과 조합해도 기권이 안 생긴다 — 전수 격자 576 구성 중 0개 |
| [0013](0013-상충쌍-보존과-상충-후보-탐지기를-범위-밖으로-둔다.md) | 상충쌍 보존 규칙과 상충 후보 탐지기를 범위 밖으로 둔다 | 컷 0.30 이 상충쌍 셋을 이미 함께 채택해 고칠 것이 0건이고, G22 에서 과채택만 는다 |
| [0014](0014-기권-게이트를-1위-top_k위-산포-축으로-채택한다.md) | 질의 단위 기권 게이트를 "1위 − `top_k`위 산포 · τ=0.06" 으로 채택한다 | 격자 165 구성 중 채택 규칙 다섯을 순서대로 통과한 것이 넷이고, 동률 규칙이 가장 단순한 하나를 고른다. τ 는 임베딩 모델에 묶인 조건 종속 인자다 |
| [0015](0015-청킹은-조항-단위를-유지하고-고정-크기는-비교까지만-한다.md) | 청킹은 조항 단위를 유지하고 고정 크기는 비교까지만 한다 | 고정 크기가 어느 조건에서도 이기지 못했고, 이겼더라도 반영은 근거 ID 체계·답변 계약·DB CHECK 를 함께 여는 별도 결정이다 |
| [0016](0016-J11-판정-픽스처를-제품-실행-경로의-근거-집합에-맞춘다.md) | J11 판정 픽스처를 제품 실행 경로의 근거 집합에 맞춘다 | 다리 조항을 비인용 근거로 넣었고 **닫히지 않았다**(0/3) — "다리 조항 부재가 원인"이 반증됐다. 대가는 측정 3 의 12세트 연속성이다 |
| [0017](0017-사유-독립-명시를-실측으로-반증하고-판정-프롬프트를-원복한다.md) | 사유 독립 명시를 실측으로 반증하고 판정 프롬프트를 원복한다 | "두 사유는 독립"이라는 사안 비특정 강화가 3회 중 1회만 닫혀 사전 판정선(≥2/3) 미달 — 미달이면 원복이 기본이다 |
| [0018](0018-판정-모델-Anthropic-3종을-비교만-하고-기본값은-바꾸지-않는다.md) | 판정 모델 Anthropic 3종을 비교만 하고 기본값은 바꾸지 않는다 | 처분은 실측 전에 정해 두었다. opus 는 sonnet 과 33칸 전부 동일해 **구분되지 않고**, haiku 는 오탐 3/15·상충쌍 1건 미검출이다 |
| [0019](0019-판정-프롬프트-캐싱을-실측하고-채택하지-않는다.md) | 판정 프롬프트 캐싱을 실측하고 채택하지 않는다 | 총 프롬프트 토큰이 전후 정확히 같고(30,042/회) 캐싱이 옮긴 것은 단가뿐이다. 집계 지표 조건이 미달이라 스위치는 꺼진 채 남기고 캐시 계열 토큰 배선만 남긴다 |
| [0020](0020-비동기화를-검토하고-이번-사이클에는-하지-않는다.md) | 처리 비동기화를 검토하고 이번 사이클에는 하지 않는다 | 헤드라인 지표를 1도 움직이지 않으면서 상태 enum·DB CHECK 셋·계약의 동치 하나를 연다. 선행 조건은 **단계별 지연 계측**이고 그건 아직 0회다 |
| [0021](0021-접기를-번호-계열-안에서만-넓힌다.md) | 탐지 전 접기를 패턴별로 두고 번호 계열 안에서만 넓힌다 | NFKC 는 `Pd` 대시류도 `Mn` 결합 표식도 접지 않아 지어낸 번호가 통과했다. 전역으로 넓히면 이메일·주소 계열까지 함께 움직이므로 접기를 패턴 속성으로 내린다 |
| [0022](0022-미정의-사유를-갈라-처분하고-검색-정렬에-tie-break를-넣는다.md) | 미정의 사유를 갈라 처분하고 검색 정렬에 tie-break 를 넣는다 | 기권 통계량이 미정의인 두 사유는 처분이 다르다 — 1위 코사인 ≤ 0 은 기권이고 측정 2건 미만은 기권이 아니다(fail-closed 를 기계적으로 걸면 정상 케이스가 죽는다). 절단선의 동점은 근거 ID 전순서가 가른다 |
| [0023](0023-자격-증명을-비밀-전용-타입으로-옮기고-기권-게이트-조립을-임베딩-조건에-묶는다.md) | 자격 증명을 비밀 전용 타입으로 옮기고, 기권 게이트 조립을 임베딩 조건에 묶는다 | `repr=False` 는 필드 표시만 가려 설정 객체를 통째 덤프하면 값이 그대로 나왔다. τ 는 `(모델, 차원)` 조건에 묶여 조립 시점에 죽는다 — 단 **명시적으로 끈 실행**은 통과시킨다(그것이 τ 재산출의 유일한 문이다) |
| [0024](0024-조회-가드의-유래-추적을-안쪽-스코프까지-내린다.md) | 조회 가드의 유래 추적을 안쪽 스코프까지 내리고 형변환 대상 타입을 허용 목록으로 판정한다 | 방향이 반대인 구멍 둘 — CTE·파생·`COALESCE` 를 거친 정상 에코가 오기각됐고, 형변환 대상 타입이 검사되지 않아 시스템 카탈로그가 근거로 들어왔다 |
| [0025](0025-단계별-지연을-구간-아홉으로-가르고-미측정을-0-과-구분한다.md) | 단계별 지연을 구간 아홉으로 가르고 미측정을 0 과 구분한다 | 계약·스키마를 넓히지 않고 평가 리포트까지만 싣는다. 돌지 않은 구간은 0 이 아니라 미측정이고, 예외로 죽은 호출의 경과도 토큰과 같은 자격으로 올려보낸다 |
| [0026](0026-실행-조건-지문을-일곱-칸-넓히고-내용-지문의-범위를-선언과-동작으로-가른다.md) | 실행 조건 지문을 일곱 칸 넓히고, 내용 지문의 범위를 "선언 + 동작"으로 가른다 | 사이클 5 가 바꾼 동작 축 넷이 지문에 한 칸도 없었다. 소스 통째 해시는 주석 한 줄에 계보가 끊기므로 **고정 탐침의 판정 결과**를 해시하고, 집합은 반드시 정렬해서 담는다 |
| [0027](0027-승격-기준선을-재등재한다.md) | 승격 기준선을 재등재한다 — 구속 줄이 다시 산다 | 현 기본값 조합의 첫 풀셋 라이브 3회를 사서 등재했고, 확인 1회가 지문 스물다섯 칸 완전 일치로 **재등재가 닫혔다는 증거**를 얻었다. 같은 확인 실행의 구속 줄 판정은 미달이고 두 사실을 함께 적는다 |
