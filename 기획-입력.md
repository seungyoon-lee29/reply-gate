# Reply-Gate — 기획 하네스 입력

> 이 파일은 **기획 하네스에 넣을 확정 사항만** 담는다. 설계·아키텍처·주차 계획은 하네스가 백지에서 만든다.
> 출처: `~/workspace/포지션찾기/프로젝트-제안.md` (2026-08-04 개정판) · 요구역량 정의는 `포트폴리오-전략.md` 1절.
> 작성 2026-08-04.

## 한 줄 정의

근거 없는 답변은 스스로 reject하는 이커머스 CS 답변 에이전트. **"초안 생성"이 아니라 "틀린 초안을 걸러내는 것"이 제품의 존재 이유.**

## 포트폴리오 내 역할

3개 카드 중 **메인**. "제품 안에서 AI 산출물을 검증하는 능력"을 담당.
커버 목표 요구역량: **1(LLM API·프롬프트), 2(RAG), 3(에이전트), 4(Python 백엔드), 6(평가·운영)**.
※ 5(혼자 출시)·7(풀스택)·10(배포)은 오르다(하이킹앱)가, 8(MCP)·9(자동화)는 Reject Bench가 주로 커버한다. 여기서 무리하게 다 넣지 않는다.

## 확정 사항 (하네스가 뒤집지 말 것)

1. **게이트가 제품의 존재 이유다.** RAG 챗봇에 검증을 덧붙이는 게 아니라, 검증이 주인공이고 RAG는 그 재료다. 데모 첫 3초는 "답변이 기각되는 순간"이어야 한다.
2. **게이트는 2층.**
   - **L1 (결정론적)** — citation 존재·스키마 준수·PII 정규식. 코드로 검사, LLM 호출 0회, 100% 재현.
   - **L2 (확률론적)** — L1 통과분만 LLM-as-a-judge가 claim 단위로 근거 대조.
   - L1을 먼저 만드는 건 일정 타협이 아니라 설계 결정. L1만으로도 게이트는 동작하므로 L2는 단일 실패점이 아니다.
3. **근거는 두 종류를 동시에 모은다.** 정책 문서 RAG + 주문 DB text-to-SQL. "한 답변에 두 근거"가 이 제품의 구조적 특징.
4. **RAG는 여기서 심화한다.** 별도 RAG 프로젝트를 만들지 않기로 했으므로, 검색 품질을 측정·개선한 이력이 이 프로젝트 안에 있어야 한다.
   - 검색 전략 비교: 벡터 단독 → 하이브리드(BM25+벡터) → 리랭킹, 단계별 recall@k·precision
   - 청킹 전략 실험: 고정 크기 vs 조항 단위 (정책 문서는 조항 경계가 의미 단위)
   - reject 사유를 "생성 문제"와 "검색 실패"로 분해해서 보고
5. **노드마다 확률론/결정론을 명시적으로 가른다.** 권한·데이터 접근·루프 종료 조건은 코드가 통제하고, 의도 해석·초안 생성·claim 대조는 LLM이 맡는다. 이 경계 자체를 README 다이어그램으로 낸다.
6. **자체 에이전트 루프 (LangGraph 미사용).** 단, README에 노드/엣지 다이어그램 + "LangGraph로 짰다면" 비교 1페이지를 넣어 프레임워크 키워드는 문서에 등장시킨다.
7. **합성 데이터.** 주문 DB 500건 + 직접 작성한 정책 문서. **일부러 모호한 조항·상충 조항·엣지 케이스를 심어** 게이트가 실제로 일하는 모습이 보이게 한다.
8. **text-to-SQL 안전장치는 스펙 필수.** read-only 계정 + 스키마 화이트리스트 + 쿼리 검증. 빼면 면접 역공 대상.

## 범위 밖 (명시적 제외)

- 파인튜닝, 자체 임베딩 학습
- 카카오·이메일 등 채널 연동 (웹 폼 + 웹훅까지만)
- "CS 챗봇"이라는 포지셔닝 자체 — 리드는 항상 "근거 없는 답변을 기각하는 에이전트"

## 확정된 스택 (2026-08-04)

- **백엔드: Python / FastAPI.** 오르다가 TS 풀스택(NestJS+RN)을 이미 증명하므로, 여기까지 TS로 가면 요구역량 4(Python 백엔드)가 통째로 빈다. 언어 두 개로 백엔드를 해봤다는 것 자체가 신호가 된다.

## 미결 (하네스가 결정)

- 프론트(대시보드) 스택, 배포 방식, n8n 사용 여부
- 벡터 저장소 선택(pgvector 전제이나 호스팅은 미정)
- 지표 목표치, 골든셋 규모

## 성공 지표 방향 (수치는 하네스가 확정)

- 게이트 도입 전후 hallucination율
- L1 필터링으로 인한 L2 호출 감소율 → 건당 비용 절감
- RAG 검색 품질 단계별 개선표
- 게이트 자체 품질: 심어둔 오류 검출률 / 정상 답변 오탐률
- p50/p95 latency, 문의 1건당 토큰 비용

## 주의

- **provenance 프로젝트의 설계는 흡수하지 않는다.** (2026-08-04 결정 — 기존 설계에 문제가 있어 폐기) citation·근거 추적은 이 프로젝트에서 백지부터 설계한다.
- 그룹바이 채용 세미나 예시 이력서가 "고객상담 AI copilot"이라 같은 소재 지원자가 늘 수 있다. 남들이 생산성 지표(응답 시간 단축)를 낼 때 이쪽은 신뢰성 지표(hallucination율·검출률)로 축을 다르게 간다.
