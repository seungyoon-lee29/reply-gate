# Reply-Gate — 기획 하네스 입력

> 이 파일은 **기획 하네스에 넣을 확정 사항만** 담는다. 설계·아키텍처·주차 계획은 하네스가 백지에서 만든다.
> 출처: 저장소 밖 개인 기획 자료(2026-08-04 개정판).
> 작성 2026-08-04.

## 한 줄 정의

근거 없는 답변은 스스로 reject하는 이커머스 CS 답변 에이전트. **"초안 생성"이 아니라 "틀린 초안을 걸러내는 것"이 제품의 존재 이유.**

## 이 프로젝트가 담당하는 것

"제품 안에서 AI 산출물을 검증하는 능력" 하나를 담당한다.
다루는 영역: **LLM API·프롬프트 / RAG / 에이전트 / Python 백엔드 / 평가·운영**.
※ 그 밖의 영역(혼자 출시·풀스택·배포·MCP·자동화)은 다른 프로젝트가 맡는다. 여기서 무리하게 다 넣지 않는다.

## 확정 사항 (하네스가 뒤집지 말 것)

1. **게이트가 제품의 존재 이유다.** RAG 챗봇에 검증을 덧붙이는 게 아니라, 검증이 주인공이고 RAG는 그 재료다. 데모 첫 3초는 "답변이 기각되는 순간"이어야 한다.
2. **게이트는 2층.**
   - **L1 (결정론적)** — citation 존재·스키마 준수·PII 정규식. 코드로 검사, LLM 호출 0회, 100% 재현.
   - **L2 (확률론적)** — L1 통과분만 LLM-as-a-judge가 claim 단위로 근거 대조.
   - L1을 먼저 만드는 건 일정 타협이 아니라 설계 결정. L1만으로도 게이트는 동작하므로 L2는 단일 실패점이 아니다.
3. **근거는 두 종류를 동시에 모은다.** 정책 문서 RAG + 주문 DB text-to-SQL. "한 답변에 두 근거"가 이 제품의 구조적 특징.
4. **RAG는 여기서 심화한다.** 별도 RAG 프로젝트를 만들지 않기로 했으므로, 검색 품질을 측정·개선한 이력이 이 프로젝트 안에 있어야 한다.
   - 검색 전략 비교: 벡터 단독 → 하이브리드(BM25+벡터) → 리랭킹, 단계별 recall@k·precision
   - 청킹 전략 실험: 고정 크기 vs 조항 단위 (정책 문서는 조항 경계가 의미 단위)
   - 임베딩 모델 비교: `3-small` vs `3-large` vs 로컬(BGE-M3) recall@k — 기본값은 `3-small`이나, 최저가를 고른 근거가 "싸서"가 아니라 "측정해보니 충분해서"여야 한다
   - reject 사유를 "생성 문제"와 "검색 실패"로 분해해서 보고
5. **노드마다 확률론/결정론을 명시적으로 가른다.** 권한·데이터 접근·루프 종료 조건은 코드가 통제하고, 의도 해석·초안 생성·claim 대조는 LLM이 맡는다. 이 경계 자체를 README 다이어그램으로 낸다.
6. **자체 에이전트 루프 (LangGraph 미사용).** 단, README에 노드/엣지 다이어그램 + "LangGraph로 짰다면" 비교 1페이지를 넣어 프레임워크 키워드는 문서에 등장시킨다.
7. **합성 데이터.** 주문 DB 500건 + 직접 작성한 정책 문서. **일부러 모호한 조항·상충 조항·엣지 케이스를 심어** 게이트가 실제로 일하는 모습이 보이게 한다.
8. **text-to-SQL 안전장치는 스펙 필수.** read-only 계정 + 스키마 화이트리스트 + 쿼리 검증. 빼면 면접 역공 대상.
9. **L2 judge는 초안 생성과 다른 모델 계열을 쓴다.** 같은 계열이면 두 가지가 겹친다.
   - **self-judging bias** — 모델은 자기 계열 출력을 관대하게 평가한다. 검출률이 실제보다 낮게 나온다.
   - **더 치명적: 생성기와 판정기의 blind spot이 겹친다.** 확정사항 7의 "일부러 심는 모호·상충 조항"을 A계열이 만들면, 그건 A계열이 자연스럽다고 여기는 형태로 만들어진다. 같은 A계열 judge는 바로 그 패턴을 정상으로 판정한다. **게이트가 실제보다 잘 동작하는 것처럼 보인다** — 이 프로젝트의 대표 지표가 통째로 오염된다.

   어느 계열을 고를지는 미결이나 **"초안 생성과 같은 계열이 아닐 것"은 확정.** 이 선택 근거 자체를 README에 싣는다(평가·운영). 확정사항 8과 같은 성격 — 빼면 "judge self-bias는 어떻게 통제했나"가 면접 역공 대상이 된다.

## 범위 밖 (명시적 제외)

- 파인튜닝, 자체 임베딩 학습
- 카카오·이메일 등 채널 연동 (웹 폼 + 웹훅까지만)
- "CS 챗봇"이라는 포지셔닝 자체 — 리드는 항상 "근거 없는 답변을 기각하는 에이전트"

## 확정된 스택 (2026-08-04)

- **백엔드: Python / FastAPI.** 다른 프로젝트에서 TS 풀스택을 이미 다뤘으므로, 여기까지 TS로 가면 Python 백엔드 경험이 통째로 빈다. 언어 두 개로 백엔드를 해봤다는 것 자체가 신호가 된다.

### 외부 API provider (2026-08-04 확정)

| 용도 | provider·모델 | 근거 |
|---|---|---|
| 임베딩 | OpenAI `text-embedding-3-small` | $0.02/1M로 최저가 — Gemini($0.15)의 1/7.5, `3-large`($0.13) 대비도 우위. 한국어 성능 무난 |
| 합성 데이터 생성 (1회성) | OpenAI 상위 모델(`gpt-5.6-sol` 급) + Batch API | 심어둘 모호·상충 조항의 품질이 게이트 지표 전체의 기반. 전량 생성해도 $10 이하라 **여기서는 아끼지 않는다** |
| 초안 생성 | OpenAI `gpt-5.6-terra` (기본값, 측정 후 조정) | 생성 파이프라인 단일화 (키 1개·SDK 1개) |
| **L2 judge** | **OpenAI가 아닌 계열 1개** | 확정사항 9 참조. 계열 선택은 미결 |

**합성 데이터 생성의 범위** — 정책 문서 초안·주문 500건·골든셋 문의. 산출물은 직접 검수 후 저장소에 픽스처로 커밋하고, 시딩·평가는 커밋된 픽스처만 사용한다(재생성 없음 — 재현성 유지). 확정사항 7의 "직접 작성"은 "LLM 보조 초안 + 직접 검수·확정"으로 읽는다.

**단가 (2026-08-04 공식 페이지 확인, $/1M 토큰)**

- OpenAI: `gpt-5.6-sol` 5/30 · `terra` 2/12 · `luna` 0.20/1.20 · `text-embedding-3-small` 0.02
- Claude: Opus 5 5/25 · Sonnet 5 **2/10**(8/31까지 도입가, 9/1부터 3/15) · Haiku 4.5 1/5
- Gemini: Flash-Lite 0.30/2.50 · Embedding 0.15
- 양쪽 다 Batch API 50% 할인, 캐시 히트 입력가의 10%
- ⚠️ Claude 4.7 이후 모델은 새 토크나이저라 **같은 텍스트가 약 30% 더 많은 토큰**. 단가표만 비교하면 왜곡된다

**Gemini를 쓰지 않는 이유** — 합성 데이터라 무료 티어의 "입력 데이터를 제품 개선에 활용 + 사람 검토" 이슈는 해소된다. 하지만 무료 티어 rate limit이 **골든셋 반복 평가**(요청이 순간적으로 몰리는 워크로드)를 막는다. 유료로 쓸 거면 임베딩 단가가 OpenAI보다 높아 이점이 없다.

**추정 비용** — 합성 데이터 생성 1회 $1~7 / 문의 1건당 $0.003~0.07(모델 등급별) / 평가 반복 포함 프로젝트 전체 $50 안쪽. **비용은 이 프로젝트의 제약 조건이 아니다.**

## 미결 (하네스가 결정)

- 프론트(대시보드) 스택, 배포 방식, n8n 사용 여부
- 벡터 저장소 선택(pgvector 전제이나 호스팅은 미정)
- 지표 목표치, 골든셋 규모
- **L2 judge 모델 계열** — "OpenAI 아님"까지만 확정. 후보 간 검출률/오탐률을 실제로 비교해서 고르는 게 바람직하다(그 비교표 자체가 평가·운영 산출물)

## 성공 지표 방향 (수치는 하네스가 확정)

- 게이트 도입 전후 hallucination율
- L1 필터링으로 인한 L2 호출 감소율 → 건당 비용 절감
  - 비용 절감의 두 번째 축으로 **프롬프트 캐싱 전후**도 같이 잰다. 시스템 프롬프트·지시문·스키마 화이트리스트 같은 고정 프리픽스가 캐시 히트 시 입력 단가의 10%로 떨어진다(검색된 정책 청크는 문의마다 달라 캐시 대상이 아님) — "게이트 설계로 건당 비용을 N% 줄였다"의 근거가 하나 더 생긴다
- RAG 검색 품질 단계별 개선표
- 게이트 자체 품질: 심어둔 오류 검출률 / 정상 답변 오탐률
- p50/p95 latency, 문의 1건당 토큰 비용

## 주의

- **provenance 프로젝트의 설계는 흡수하지 않는다.** (2026-08-04 결정 — 기존 설계에 문제가 있어 폐기) citation·근거 추적은 이 프로젝트에서 백지부터 설계한다.
- "고객상담 AI copilot"은 흔한 소재라 같은 소재가 겹치기 쉽다. 대개 생산성 지표(응답 시간 단축)를 낼 때 이쪽은 신뢰성 지표(hallucination율·검출률)로 축을 다르게 간다.
